【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print）
【这一单只改一个文件】`scripts/ingest/jc_read.py`（**现 35 行，全文已在 D 节贴出**）——把批归属升级成「**`.done` 清单优先、窗口兜底**」+ **行数对账**。
 上一版工单只给了行为描述，工人读了 8 分钟还没落笔 ⇒ 这次**代码骨架直接给你**：你的活是**照 D 节抄进去 + 跑 E 节 7 条验收 + 交报告**。
 允许你在细节上改（变量名、换行、logger 提到模块顶部等），**但行为与返回值语义不许变**。
【🅣 只准读 1 个文件】`scripts/ingest/jc_read.py`（其余材料本单全内联）。**第 1 个动作就 write/edit**。

【A. 事实（照抄，别去查）】
 · 新格式 `.done` = **文本清单**，每行 `<相对路径>\t<声明行数>\t<级联哈希>`；例（2026-09-16 08:33 批真件）：
   `jczq_offer/2026-09-16T00-33Z__001.jsonl\t40\ta803738e…` / `jclq_offer/2026-09-16T00-33Z__001.empty\t0\t`（**.empty 行第三列为空**）
 · **老 3 批的 `.done` 是 0 字节**（`14-40-02Z` / `15-03-45Z` / `15-16-32Z`）⇒ **必须保留窗口兜底**。
 · 第三列"级联哈希"**算法尚未定义** ⇒ 本单**不校验**它（只校验行数）。
 · 装载器 `jc_load.load_topic(cur, root, marker, topic, path, state)` 的 `state` 语义（**按这个语义返回，别去改 jc_load.py**）：
   `state == "jsonl"` ⇒ 读行入库；`state == "missing"` ⇒ 记 `#MISSING` gap；**其它任何值**（`empty` / `rejected`）⇒ `n=0` 不读行、不算 gap。
【B. 要实现的三条行为】
 1) `.done` 非空 ⇒ 该批**只认清单**：清单里每个 topic 的每个文件都要处理；**清单完全没出现的 topic ⇒ `[(None, "missing")]`**（与今天 gap 语义一致）；
 2) **行数对账**：实际非空行数 != 清单声明行数 ⇒ **整条不入库**（返回 `[(path, "rejected")]`）+
    `logger.warning("manifest-mismatch topic=%s 声明=%d 实际=%d 文件=%s", ...)`；
    清单列出但**磁盘上没有**该文件 ⇒ `[(path, "rejected")]` + `logger.warning("manifest-file-missing …")`（半包必须看得见）；
 3) `.done` 为空（0 字节）⇒ **走 D 节那段原窗口逻辑，一字不改**。
 ⚠️ "清单外但同目录存在"的文件本单**不管**（既不许并进别的批，也不许顺手做 orphan 规则——那是下一单 `P0-COLLECT2q`）。
【C. 边界（越界即拒收）】只准改 `scripts/ingest/jc_read.py`；**禁止**改 `jc_load.py` / `jc_write.py` / `parse_*.py` / DDL / `tests/**`；
 **禁止**往 `/srv/league-staging/**` 写任何东西（那是国内机的地盘，只读）；演练一律在 `/tmp/…` 自建目录；禁 git；禁 print。

【D. 现状全文（35 行，照此改；`files_for_batch` 签名与返回类型**保持不变**）】
```python
 import json
 import re
 from datetime import datetime
 from pathlib import Path


 def _stamp(name: str) -> datetime:
     m = re.search(r"(\d{4})-(\d\d)-(\d\d)T(\d\d)-(\d\d)(?:-(\d\d))?", name)
     return datetime(int(m.group(1)), int(m.group(2)), int(m.group(3)),
                     int(m.group(4)), int(m.group(5)), int(m.group(6) or "0"))


 def iter_markers(root: Path, only: str | None = None) -> list[Path]:
     ms = sorted(root.glob("*.done"))
     key = only + ".done" if only and not only.endswith(".done") else only
     return [m for m in ms if m.name == key] if key else ms


 def files_for_batch(root: Path, marker: Path,
                     topics: tuple[str, ...]) -> dict[str, list[tuple[Path, str]]]:
     ms = iter_markers(root)
     i = ms.index(marker)
     lo, hi = (_stamp(ms[i - 1].name) if i else datetime.min), _stamp(marker.name)
     out = {}
     for topic in topics:
         cands = [p for pat in ("*.jsonl", "*.empty")
                  for p in (root / topic).glob(pat) if lo < _stamp(p.name) <= hi]
         cands.sort(key=lambda p: p.name)
         out[topic] = [(p, p.suffix.lstrip(".")) for p in cands]
     return out


 def read_lines(path: Path) -> list[dict]:
     return [json.loads(s) for s in open(path, encoding="utf-8") if s.strip()]
```
【D2. 建议骨架（总长 ≤95 行；`from core.log import logger`）】
```python
 def _count_lines(path: Path) -> int:                 # 非空行数，口径必须与 read_lines 一致
 def _manifest(root: Path, marker: Path) -> dict[str, list[tuple[Path, int]]]:
     """.done 清单 → {topic: [(绝对路径, 声明行数)]}；空 marker ⇒ 返回 {} 表示"该走窗口"。
        解析要容错：制表符分隔、允许第三列为空、跳过空行；rel 的第一段就是 topic。"""
 def _from_manifest(root, marker, topics) -> dict[str, list[tuple[Path, str]]]:
     """按 B 节三条实现。"""
 def files_for_batch(root, marker, topics):            # 签名不变
     man = _manifest(root, marker)
     return _from_manifest(root, marker, topics) if man else <原窗口逻辑一字不改>
```

【E. 必须成立——7 条，全部用**真目录**跑（队长 08:36Z 已把这些数字钉死）】
 E1 **老三批（0 字节 marker ⇒ 窗口分支）行为不许变**：
    `cd /home/ubuntu/league-v2/repo && source ~/.venvs/league/bin/activate && PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /tmp/incoming`
    → 三行 batch 日志 `errors=[]`，`ups=` 必须仍是 **`14-40-02Z ups=85` / `15-03-45Z ups=85` / `15-16-32Z ups=15`**
 E2 **新批（清单分支）**：`PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /tmp/incoming2`
    → 两行 `errors=[]`，`ups=` 必须 **`23-01-30Z ups=40` / `23-03-19Z ups=40`**（清单各 40 行盘口，5 个 `.empty` 的 topic **不算 gap**）
 E3 **实时落港目录**（11+ 个 marker；最后一批可能在你跑的时候刚出现）：
    `PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /srv/league-staging/incoming/cn-collector`（**只读**）
    → 每批 `errors=[]`；`23-13-19Z / 00-03-20Z / 00-13-19Z / 00-23-19Z / 00-33-18Z` 各 **`ups=49`**（40 盘口 + 9 赛果）；
    ⚠️ **`23-01-30Z` 这一批在清单口径下 `ups` 必须从 160 变成 40**——旧窗口口径把 `22-48Z / 22-49Z / 22-54Z` 三个**清单外**文件并了进来。
    这条差异**就是本单存在的意义**，报告里必须写；**数据不会丢**（那三批已在库里 ⇒ `fact.jc_offer` 的 count 不会下降）。
 E4 **count 不许下降**：跑 E3 前后各查一次
    `sudo -n -u postgres psql -d league -Atc "select count(*) from fact.jc_offer"` 与 `... from fact.jc_result`
    ⇒ 两个数**跑后 ≥ 跑前**（相等即可；**变小就是清单分支漏读** ⇒ 贴原文停下）
 E5 **行数对账必须有实测**：只在 `/tmp/mmtest/` 自造一批：`jczq_offer/` 放一个真件（从 `/tmp/incoming2/jczq_offer/` 抄一份，**别改原包**），
    `.done` 里把该行的声明行数写成 `99` ⇒ 跑 `--dir /tmp/mmtest`：**日志出现 `manifest-mismatch … 声明=99 实际=4 …`** 且该批 `ups=0`。
 E6 静态：`python3 ~/bin/omp-ast-check.py scripts/ingest/jc_read.py scripts/ingest/jc_load.py scripts/ingest/jc_write.py` → **0 违规**（`jc_read.py` ≤100 行）
 E7 回归：`PYTHONPATH=scripts python -m pytest tests -q` → **428 passed / 0 failed**
【铁律】① 禁 git；② 禁 print；③ ≤100/≤50/≤120；④ 只改 `jc_read.py`；
 ⑤ 报告 `.omp-logs/P0-COLLECT2o.report.txt`：**E1~E7 原始命令 + 原始输出逐条贴**，开头一行结论
 + 只有全绿才 `echo TASK_DONE > .omp-logs/P0-COLLECT2o.done`
【边界】任何一条对不上：**贴原文 + 停下**，不许为了对上数字改 `jc_load.py`、改库、改包、或放宽 E 节期望。跑不绿也是有效证据。
【验收（队长逐字复跑）】
  bash ~/bin/jc-ingest-run.sh
  LEAGUE_FROZEN_RE="scripts/core/" bash ~/bin/league-accept.sh P0-COLLECT2o scripts/ingest
