【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print）
【这一单**只新建两个文件**：`scripts/core/jc_gate.py`（**≤45 行**）+ `tests/test_jc_gate.py`——不接线、不改任何现有文件】
 它是"**封闭清单闸门**"：用户 09-16 06:55 定死"只预测 竞彩足球 英超/西甲/意甲/德甲/法甲/中超 + 篮彩 NBA/CBA；数字彩只对照不预测"，
 而 §9-76 实测：**今天在售的竞彩足球 36 场里，清单内只有"西甲 9 场"**（其余是欧罗巴/解放者杯/英联赛杯/亚冠精英/亚运/荷甲/巴甲/英冠）
 ⇒ **没有这道闸门，将来任何 JC 预测器都会把清单外联赛一起预测**（那是越界，比不预测更糟）。
【🅣 第 1 个动作就 write；**判据全部离线自量**（§9-75：cron 会动的数一律不许写死，只有夹具/常量可写死）。】
【A. 事实（队长 12:53 从运行库逐 `league_id` 实测，**照抄，别自己去查**）】
 `fact.jc_match` 的 `league_cn` **恒为全称**、`league_abbr` **恒为缩写**、成对出现（不冲突，§9-76）；今天在场 10 组：
 | league_id | league_cn | league_abbr | 场数 | 清单内？ |
 |---|---|---|---|---|
 | 70 | 欧罗巴联赛 | 欧罗巴 | 12 | ✗ |
 | **62** | **西班牙甲级联赛** | **西甲** | **9** | **✔** |
 | 49 | 南美解放者杯 | 解放者杯 | 4 | ✗ |
 | 24 | 英格兰联赛杯 | 英联赛杯 | 3 | ✗ |
 | 1 | 亚洲冠军精英联赛 | 亚冠精英 | 2 | ✗ |
 | 83 | 亚运会男足 | 亚运男足 | 2 | ✗ |
 | 17 | 荷兰甲级联赛 | 荷甲 | 1 | ✗ |
 | 6 | 巴西甲级联赛 | 巴甲 | 1 | ✗ |
 | 106 | 亚运会女足 | 亚运女足 | 1 | ✗ |
 | 20 | 英格兰冠军联赛 | 英冠 | 1 | ✗ |
【B. 要写的东西（就这么点，**别自由发挥**）】
```
ALLOW_ABBR: frozenset[str]     # 足球 {英超,西甲,意甲,德甲,法甲,中超} ∪ 篮彩 {NBA,CBA} ⇒ 共 8 个
ALLOW_IDS:  frozenset[int]     # 已实测确认的数字 id：今天只有 {62}；将来观测到别的联赛 id 由**队长**增补，工人不许自己加
def is_predictable(league_abbr: str | None, league_id: int | None = None) -> bool
    # 只认 ALLOW_ABBR（缩写）；league_id **不参与判定**，但传进来若命中 ALLOW_IDS 也必须放行（两条路任一命中即 True）
def partition(rows) -> tuple[list, list]   # rows = [{league_abbr, league_id}, …] ⇒ (可预测, 不可预测)，保持原顺序
```
 B1 **未知/空/异常输入一律 `False`**（`None`、`''`、`'  西甲 '` 去空白后仍要能认 ⇒ `strip()` 一次）；
 B2 **绝不允许"清单外靠猜测放行"**（红线：宁可不插，不可插错）；**不许**在本文件里写 SQL、import 任何 `store.*`（纯函数，§9 分层）；
 B3 顶部 docstring 必须写清楚：**这道闸门只决定"进不进模型"，不决定"进不进库"**（清单外照入库当走势资料）。
【C. 不许做】不许改 `jc_view.py` / `jc_load.py` / `jc_read.py` / `jc_write.py`(md5 锁) / `parse_odds_hist.py` / 任何 DDL / 任何夹具；
 不许往库里写任何东西；不许碰 `ref.team`（D3）；禁 git；禁 print；禁 `requests/urllib`。
【T. 必须成立（按序跑，原始输出全进报告）】
 T1 **对照 A 表逐个断言**（`tests/test_jc_gate.py` 里把 10 组 (id, abbr) 写成常量）：
    `is_predictable('西甲', 62) is True`；其余 9 组**全部 False**；且 `sum(判定为 True 的行) == 1`（**只有西甲**）。
 T2 **篮彩**：`is_predictable('NBA')` / `('CBA')` → True；`is_predictable('WNBA')` / `('欧洲篮球联赛')` → False。
 T3 **异常输入**：`is_predictable(None)`、`('')`、`('   ')`、`('西甲   ')`(带空白 → True)、`('西甲联赛')`(**不在白名单的相似写法 → False**) → 全不抛异常。
    ⚠️ 最后这条是**故意**的：'西甲联赛' 这种没见过的写法**必须拒**（不许做模糊匹配，模糊匹配就是"插错"的入口）。
 T4 **`partition`**：喂 A 表 10 组 ⇒ `(1, 9)`；喂空列表 ⇒ `([], [])`；顺序不变（贴前后两个 `league_abbr` 序列）。
 T5 **静态与回归**：`python3 ~/bin/omp-ast-check.py scripts/core/jc_gate.py tests/test_jc_gate.py` → 0 违规；
    `wc -l scripts/core/jc_gate.py` → **≤45**；`source ~/.venvs/league/bin/activate && PYTHONPATH=.:scripts python -m pytest tests -q` → **`0 failed`**
    （**基线数字按你实际跑出来的贴**，队长 12:52 是 435 passed；§9-75：并行工单会加用例，所以这里**不写死**）
 T6 **闸门与真库对一次数**（只读！）：
    `sudo -n -u postgres psql -d league -Atc "select league_abbr, count(*) from fact.jc_match group by 1 order by 2 desc"` 贴原文
    ⇒ 再把你 T1 的结论与它并排写一句"**清单内 = 西甲 9 场，其余 27 场不进模型**"（数字对不上就贴实测，别改判据）。
【铁律】① 禁 git；② 禁 print；③ ≤45 行、函数 ≤50、列 ≤120；④ 只新建两个文件；
 ⑤ 报告 `.omp-logs/P0-JCGATE1.report.txt`（T1~T6 原始命令与输出 + `wc -l`）+ 全绿才 `echo TASK_DONE > .omp-logs/P0-JCGATE1.done`
【边界】`ALLOW_IDS` 只许 `{62}`（其余 id 我还没观测到 ⇒ 你**不许**自己填"看起来像英超的 id"）；T1/T2/T3 任一断言写不出真值 ⇒ 停下贴实测；超 45 行 ⇒ 停下贴 `wc -l`。
【验收（队长逐字复跑）】
  PYTHONPATH=.:scripts python -m pytest tests/test_jc_gate.py -q
  python3 ~/bin/omp-ast-check.py scripts/core/jc_gate.py
  LEAGUE_FROZEN_RE="scripts/(core/leagues|store/|ingest/)" bash ~/bin/league-accept.sh P0-JCGATE1 scripts/core/jc_gate.py tests/test_jc_gate.py
