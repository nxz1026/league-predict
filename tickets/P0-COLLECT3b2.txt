【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print、不许放宽红线）
【这一单**只新建一个写手**：`scripts/ingest/jc_odds_write.py`（**≤100 行、函数 ≤50 行**）——不接线、不改任何现有文件、**库里最终不留行**（见 T2 的"回滚自证"）】
 它是 `P0-COLLECT3b`（盘口**全历史** → `fact.jc_odds_history`）第二步；第一步（解析器 `scripts/store/parse_odds_hist.py`）**已单独成单 `P0-COLLECT3b1`**（本单**假定它已存在并按其输出喂**；若你发现它不在 ⇒ 停下告诉我）。
【🅣 第 1 个动作就 write 这个新文件 + 一个新测试文件；**基线我全量好了，你不要自己去测基线**（§9-69）。】
【🛑 **幂等保护（队长 14:47 加，专治"重试会话重写已验收代码"，§9-84）**：开工前先跑
   `ls scripts/ingest/jc_odds_write.py tests/test_jc_odds_write.py 2>/dev/null | wc -l`，再跑
   `source ~/.venvs/league/bin/activate && PYTHONPATH=.:scripts python -m pytest tests/test_jc_odds_write.py -q`
   ⇒ **若两个文件都在且已经 `2 passed`：本单剩下的活只有"补报告 + 交 `.done`"，一个字节都不许多改**
   （写手与测试已由队长验收进 commit `a17f798`；重写只会浪费一轮，并把已绿的实现置于风险中）。
【A. 表与上游（队长 11:55 从运行库读的，照抄别去查）】
 `fact.jc_odds_history`（**14 列**，DDL 见 `docs/db/infra_p0_11_odds_history.sql`，**已应用**）：
   `match_id bigint NOT NULL` · `play_type text NOT NULL` · `update_ts timestamptz NOT NULL` · `seq_no integer NOT NULL`
   · `goal_line text` · `goal_line_value numeric` · `odds jsonb NOT NULL` · `is_first boolean NOT NULL` · `is_close boolean NOT NULL`
   · `league_id integer` · `src_hash text NOT NULL` · `src_file text NOT NULL` · `first_seen_at timestamptz NOT NULL default now()` · `last_seen_at timestamptz`
 **PK = `(match_id, play_type, update_ts)`**；CHECK：`play_type IN ('had','hhad','crs','ttg','haf')`、`jsonb_typeof(odds)='object'`、`seq_no >= 0`；
 **故意没有外键**（§9：装载链路不依赖 `ref.team`，D3）；授权：`league_ing` 只有 SELECT/INSERT/UPDATE（**没有 DELETE**）。
 上游 `parse_odds_hist.parse_env(env)` 每行返回 dict，**键就是上面这些列名**（`odds` 是 **Python dict**、`update_ts` 是带 tzinfo 的 `datetime`、`seq_no` 0=最早、`is_first` 只在 `seq_no==0` 为 True、`is_close` 恒 False）。
【B. 公开面（就两个名字）】
```
def upsert_jc_odds_history(cur, rows: list[dict], src: dict) -> int   # 返回"真正影响行数"合计；单条坏行不许炸整批（见 B3）
def upsert_from_env(cur, env: dict) -> int                            # 调 parse_odds_hist.parse_env 后转调上面，src 从信封取
```
 B1 语句形状（**照这个写，别改列顺序**）：
  `insert into fact.jc_odds_history (match_id,play_type,update_ts,seq_no,goal_line,goal_line_value,odds,is_first,is_close,league_id,src_hash,src_file)`
  ` values (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)`
  ` on conflict (match_id,play_type,update_ts) do update set seq_no=excluded.seq_no, goal_line=excluded.goal_line,`
  ` goal_line_value=excluded.goal_line_value, odds=excluded.odds, is_first=excluded.is_first, is_close=excluded.is_close,`
  ` league_id=excluded.league_id, src_hash=excluded.src_hash, src_file=excluded.src_file, last_seen_at=now()`
  ⇒ **`first_seen_at` 不在 do-update 列表里**（首次插入靠 default now()，重复装载只刷 `last_seen_at`）。
 B2 `odds` 是 jsonb ⇒ **必须 `Json(row["odds"])` 包**（psycopg3 直接塞 dict 会被当 Python 对象报错；这是仓库里踩过的坑）。
 B3 **坏行不许炸整批 —— 但必须用 SAVEPOINT**（队长 §9-78 补：我原来只写"逐行 try/except"，**在同一个 PG 事务里那样根本不够**：
     任何一条语句报错后事务就进入 aborted 状态，后续语句全部报 `InFailedSqlTransaction`（= 你现在测试里那两条 `ERROR ... psycopg`）。
     正确形状（**照这个改，只多两行**）：
       每条语句前 `cur.execute("savepoint rh")`；成功则 `cur.execute("release savepoint rh")` 并 `n += cur.rowcount or 0`；
       失败则 **`cur.execute("rollback to savepoint rh")`**（把事务救回可用）+ `logger.warning("odds-hist-reject …")` + `continue`；
     返回的 int = 成功行数；**不许**吞异常后返回 0 还打 TASK_DONE。
     ⚠️ T3 的三条（1 好 2 坏）必须返回 **1** 且**不抛**，同时 `fact.jc_odds_history` 里那条好的**在事务内可查到**（T3 也在回滚事务里跑）。
 B4 `is_close` **原样透传**（上游恒 False）——**不许在这一层猜"最后一版就是收盘价"**（§9-70：v1.4 载荷 16 个键里没有任何状态字段，我量过）。
【C. 不许做】不许 `ALTER`/建表（表已存在）；不许改 `jc_write.py`（md5 锁）/`jc_load.py`（`P0-COLLECT2q3b` 正在改）/`jc_read.py`/DDL/夹具；
 **不许往 `fact.jc_odds_history` 最终留行**（T2 用回滚证明）；不许碰 `ref.team`（D3）；不许写 `/srv/league-staging/**`；禁 git；禁 print；演练一律 `/tmp/…`。
【T. 必须成立（按序跑，原始输出全进报告）】
 T1 **先算期望**（用解析器在本地跑两份夹具，不落库）：合计 **34 行**（had 7 · hhad 9 · crs 6 · ttg 6 · haf 6；`finished` 29 / `onsale` 5）
 T2 **回滚自证（本单核心）**：写一个测试（`tests/test_jc_odds_write.py`），
    用 `with closing(pg.connect("ing")) as conn:` 打开事务 ⇒ `upsert_from_env` 喂两份夹具（**34 行**）⇒ 断言
    ⚠️ **连接生命周期（队长 §9-79 补，专门治 `the connection is closed` + `assert 0 == 34` 这一对症状）**：
      · 全程**只用一个 conn**，且**所有断言都在 `conn.close()` 之前**完成（`with closing(...)` 块**内部**跑完 T2 与 T3 再退出）；
      · **不要**在 `with` 块外面再碰 cursor/conn；**不要**在 `rollback()` 之后还去 `cur.execute` 查数（查数要么在 rollback **之前**（看到未提交数据），
        要么 rollback **之后**重开一个只读连接查（看到 0 行）——**两种别混在一条断言里**；
      · `assert 0 == 34` 的另一半原因通常是**每条 insert 都被 B3 的容错路径吞掉**（真错误被 `logger.warning` 藏了）
        ⇒ 请在测试里**先把容错关掉**（直接 `cur.execute` 一条）确认能插入 1 行，再回头测 34 条。
      `select count(*) from fact.jc_odds_history` == **34**、`select count(*) filter (where is_first) ...` == **10**、
      `select count(*) filter (where is_close)` == **0**、`select count(*) filter (where goal_line is not null)` == **9**、
      重放同一批第二遍 ⇒ 行数**仍是 34**（PK upsert 生效）且 `last_seen_at` 非空、`first_seen_at` 不变（贴两条时间戳原文）
    ⇒ **`conn.rollback()`** ⇒ 再 `select count(*) from fact.jc_odds_history` == **0**（**证明库里最终不留行**）
 T3 **坏行不炸批**：构造 3 条（1 条正常、1 条 `play_type='xxx'` 违反 CHECK、1 条 `odds` 是 list 违反 `jsonb_typeof='object'`）
    ⇒ 函数**不抛异常**、返回 **1**、日志出现 `odds-hist-reject`（原始输出贴回来）；**同样在回滚事务里跑**
 T4 **静态**：`python3 ~/bin/omp-ast-check.py scripts/ingest/jc_odds_write.py tests/test_jc_odds_write.py` → 0 违规；`wc -l`（写手 **≤100**、函数 ≤50）
 T5 **回归**：`source ~/.venvs/league/bin/activate && PYTHONPATH=.:scripts python -m pytest tests -q` → **428 + N passed / 0 failed**（贴你数出的 N）
    且 `bash ~/bin/jc-ingest-run.sh` → `rc=0`、`select count(*) from ops.file_arrival where src_file like '%#MISSING%'` **跑前跑后都 = 14**
【铁律】① 禁 git；② 禁 print；③ ≤100/≤50/≤120；④ 只新建"写手 + 它的测试"两个文件；
 ⑤ 报告 `.omp-logs/P0-COLLECT3b2c.report.txt`（T1~T5 原始命令 + 原始输出 + 两个文件行数）+ 全绿才 `echo TASK_DONE > .omp-logs/P0-COLLECT3b2c.done`
【边界】T2 任何一数对不上 ⇒ **停下贴实测值**（不许改断言凑绿）；`parse_odds_hist.py` 不存在 ⇒ 停下；写不进 100 行 ⇒ 停下贴 `wc -l`。
【验收（队长逐字复跑）】
  sudo -n -u postgres psql -d league -Atc "select count(*) from fact.jc_odds_history"        # 必须仍是 0
  PYTHONPATH=.:scripts python -m pytest tests -q
  python3 ~/bin/omp-ast-check.py scripts/ingest/jc_odds_write.py
  LEAGUE_FROZEN_RE="scripts/(core|store/pg|ingest/jc_write|ingest/jc_read|ingest/jc_load)\.py" bash ~/bin/league-accept.sh P0-COLLECT3b2 scripts/ingest tests/test_jc_odds_write.py

【🔎 队长 13:22 亲自诊断出的**唯一真因**（我跑 `pytest -k t2 --log-cli-level=WARNING` 抓到被容错吞掉的原文）：
 `odds-hist-reject match_id=2041482 play_type=had: Connection.transaction() got an unexpected keyword argument 'savepoint'`
 ⇒ 你用的是 **`conn.transaction(savepoint=...)`，psycopg3 没有这个关键字**（`Connection.transaction()` 只收 `force/readonly/deferrable/isolated`）；
 ⇒ **改用裸 SQL savepoint**（就是我 B3 写的三行）：`cur.execute("savepoint rh")` / 成功 `cur.execute("release savepoint rh")` / 失败 `cur.execute("rollback to savepoint rh")`；
 ⇒ 除此之外**代码与测试都别动**（`assert 0 == 34` 与 `the connection is closed` 都是这一条的连带症状：坏行被容错吞成 0 成功，测试收尾又在 close 之后碰了 cursor）。
【🎯 13:42 追加（队长现场看到的**当前**错误写法，新会话请**第一刀就改掉它**）：
 现在 `scripts/ingest/jc_odds_write.py:34` 是 `with cur.transaction(savepoint=True):` —— **psycopg3 的 Cursor 根本没有 `transaction()`**
 （上一版是 `conn.transaction(savepoint=...)`，同样不存在这个关键字）⇒ 每条 insert 都被容错吞掉 ⇒ 测试仍 `assert 0 == 34`。
 ⇒ **唯一正确写法（不要再试任何 `*.transaction(savepoint=…)` 变体）**：
   `cur.execute("savepoint rh")` → 执行 insert → 成功 `cur.execute("release savepoint rh")` → 失败 `cur.execute("rollback to savepoint rh")` + `logger.warning` + `continue`。
 （psycopg3 里"嵌套事务自动开保存点"只有 `with conn.transaction():`，但它**不接受 `savepoint=` 参数**，而且放进 for 循环会让函数超 50 行 ⇒ 一律用裸 SQL。）
【🩹 14:09 追加：`savepoint` 你已经改对了（我 grep 到 3 处、`assert 0 == 34` 也消失了），**现在只剩测试的连接收尾这一个错**：
 · 症状：`psycopg.OperationalError: the connection is closed`，位置是 fixture 收尾的 `connection.rollback()`；
 · 根因：你在 **fixture 里 `with closing(connection)`**、又**在每个用例里再来一次 `with closing(conn)`** ⇒ **同一条连接被关两次**，
   第一个 `closing` 关闭后，第二个 `rollback()` 就打在已关闭的连接上（用例中间的断言也因此看不到未提交的数据）。
 · `store/pg.py:32` 的 `connect()` 契约是"**裸连接、调用方自己管事务**"⇒ **只允许一个所有者**。修法（**照抄，别自己发明**）：
```python
@pytest.fixture()
def conn():
    connection = pg.connect("ing")
    try:
        yield connection            # 用例里**不要**再套 closing / with
    finally:
        connection.rollback()       # 先回滚（库里最终 0 行的保证）
        connection.close()          # 再关
```
   用例体内：`cur = conn.cursor()` 直接用；所有断言**都在 `finally` 之前**跑完；查"库里最终 0 行"那一步
   **另开一条只读连接**（`with pg.read_conn("ro") as c:`）——**不要在 rollback 之后复用同一条连接去查**。
【🩹 14:14 再追加两条（若你正在跑，忽略此段；这是给下一轮会话的）：
 ① `'Connection' object has no attribute 'rowcount'` ⇒ **测试传错了对象**：写手收的是 **cursor**（B1 签名 `upsert_jc_odds_history(cur, rows, src)`），
    你写成 `upsert_from_env(conn, ENVS[0])` ⇒ 必须 `cur = conn.cursor()` 后**全程传 `cur`**（`cur.rowcount` 才是每语句行数）；
 ② `InvalidSavepointSpecification: savepoint "rh" does not exist` ⇒ **保存点必须"本轮先建后用"**：
    每一次循环内 `cur.execute("savepoint rh")` → 试 insert → 成功 `release savepoint rh` / 失败 `rollback to savepoint rh`；
    **不许**把 `savepoint` 建在循环外（第一条失败之后就再也 release 不了，第二条就报"does not exist"）。
 ③ 判据不变：T2 = **34 → 重放仍 34 → rollback → 另开 `pg.read_conn("ro")` 查 = 0**；T3 = 三条(1 好 2 坏) 返回 **1** 且不抛。