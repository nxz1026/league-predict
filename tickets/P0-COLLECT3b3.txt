【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print）
【这一单是"盘口全历史"**接线**（第三步，也是最后一步）：把 `jc_odds_history` 这个 topic 接进入库链路——**净增 ≤14 行，分散在 3 个文件**】
 前置（都已完成，别重做）：`3b1` 解析器 `scripts/store/parse_odds_hist.py`(74) ✔ · `3b2` 写手 `scripts/ingest/jc_odds_write.py` ✔ · `P0-JCSPLIT` 把
 `jc_load.py` 从 99 搬到 **66 行**（`_ops`/`load_topic` 现在在 `scripts/ingest/jc_topic.py`）✔ ⇒ **这两个文件都已验收（`scripts/store/parse_odds_hist.py` 74 行 · `scripts/ingest/jc_odds_write.py` 51 行，`pytest 441 passed`）；**若你发现它们不在，停下告诉我****。
【🅣 第 1 个动作就 edit（三个小改动，一次写完再跑测试）；判据一律"自量自比"（§9-75：cron 每 10 分钟动一次数，写死必过期）。】
【A. 为什么必须特殊处理（**这是本单唯一有难度的地方，读懂再动手**）】
 `jc_odds_history` 是**一天一趟**（契约 v1.4，对方 15:00 档采一次），而装载 cron 是 **`*/10` 一天 144 趟**。
 现在 `jc_read.py` 的 `TOPICS` 是**记账白名单**：`_from_manifest()` 对 `TOPICS` 里**每个** topic 都做
 "清单没列 ⇒ `out[topic] = [(None,'missing')]` ⇒ 落一条 `#MISSING` gap"。
 ⇒ **如果直接把新 topic 塞进 `TOPICS`，每天会凭空多出 143 条假缺档**（把 2q3 刚修干净的账本又搞脏，这是我明确不允许的）。
 ⇒ 所以要新增一个**可选 topic** 概念：**只有清单里真的列了它，才处理；没列就当不存在，绝不记 gap**。
【B. 三处改动（照抄，别扩）】
 B1 `scripts/ingest/jc_read.py`（97 行 ⇒ ≤100）：
   · 加一行常量 `OPTIONAL = ("jc_odds_history",)`；
   · `_from_manifest()` 结尾**在现有 `for topic in topics:` 之后**加一小段：对 `OPTIONAL` 里、且在 `resolved`/`man` 中出现过的 topic，
     按 `_check_entry` 同样口径补进 `out[topic]`（**没出现就什么都不做**）。**不许**改 `TOPICS` 本体、**不许**动 `_check_entry`/`_resolve`/`_window`。
 B2 `scripts/ingest/jc_topic.py`（44 行 ⇒ ≤60）：`load_topic()` 里在 `if topic in WRITE:` 之后加 `elif topic == "jc_odds_history":` 分支
   —— 逐行 `env` 调 `jc_odds_write.upsert_from_env(cur, env)` 累加 `ups`；单行异常 ⇒ `rej.append({"line": i, "reason": f"{type(e).__name__}:{str(e)[:120]}"})` 继续
   （**坏行不炸批**，与 3b2 B3 同口径）；顶部补 `from ingest import jc_odds_write`。**不许**把 `jc_odds_history` 加进 `WRITE`（那是"双表 upsert"的名单，语义不同）。
 B3 `scripts/ingest/jc_load.py`（66 行 ⇒ ≤80）：**只**把 orphan 兜底那两行的 topic 白名单从 `TOPICS` 扩成 `TOPICS + OPTIONAL`
   （孤儿回收也认盘口历史目录），**别的一律不许动**。
【C. 不许做】不许建表/`ALTER`（`fact.jc_odds_history` 已在）；不许改 `jc_write.py`(md5 锁)/`jc_odds_write.py`/`parse_odds_hist.py`/任何 DDL/任何夹具；
 不许碰 `ref.team`（D3）；不许写 `/srv/league-staging/**`；不许往真库**最终留行**（T3 用临时库/回滚证明）；禁 git；禁 print。
【T. 必须成立（按序，原始输出全进报告）】
 T0 **自量基线**（改之前）：`bash ~/bin/jc-ingest-run.sh` 之后立刻记 `A1=orphan n=?`、`A2=gaps=?`、`A3=fact.jc_odds_history 行数=?`（**必须是 0**）。
 T1 **假 gap 不许增长（本单核心）**：改完之后**再跑一次** `bash ~/bin/jc-ingest-run.sh`
    ⇒ `gaps` **仍 = A2**（±0；若 `+143` 就是你把它塞进 `TOPICS` 了 ⇒ 停下重做）、`orphan n` 与 A1 同、`errors=[]`。
 T2 **可选 topic 真被处理**：临时演练目录 `/tmp/oh/`（**别碰 staging**）——
    造一个 `oh/jc_odds_history/2026-09-17T06-00-00Z__001.jsonl`（内容 = 把 `tests/fixtures/odds_history/` 里**两份夹具原文**逐行拷进来，**别改一个字节**）
    + `oh/2026-09-17T06-00-28Z.done` 只写一行 `jc_odds_history/2026-09-17T06-00-00Z__001.jsonl\t34`；
    ⇒ `PYTHONPATH=.:scripts python scripts/ingest/jc_load.py --dir /tmp/oh --batch 2026-09-17T06-00-28Z`
    ⇒ 期望：日志出现 `jc_odds_history` 且 `rows=34`；`select count(*) from fact.jc_odds_history` = **34**；
      `select count(*) filter (where is_first) ... , count(*) filter (where goal_line is not null), count(*) filter (where is_close)` = **10 / 9 / 0**；
      **再跑一遍同一条命令** ⇒ 行数**仍是 34**（PK upsert）、`gaps` 与 T1 相同（**这个演练不许造 gap**）。
 T2b **并发提示（队长 14:28 补）**：`P0-COLLECT3b2c` 的测试可能同时在往 `fact.jc_odds_history` 里"开事务→回滚"，
   你若在 T2/T3 的 SQL 上**卡住超过 30 秒**（`select pid, state, wait_event_type from pg_stat_activity where datname='league'` 能看到 `lock`），
   **不要杀别人的事务**，等它跑完再来一次；实在等不到就把 T2/T3 挪到报告最后再做，并贴出等待证据。
 T3 **收干净**：`sudo -n -u postgres psql -d league -c "delete from fact.jc_odds_history"` ⇒ 行数回到 **A3(=0)**；
    并 `delete from ops.file_arrival where src_file like '%2026-09-17T06-00%'` ⇒ 演练痕迹清零（贴两条 `DELETE n` 原文）。
 T4 **静态**：`python3 ~/bin/omp-ast-check.py scripts/ingest/jc_read.py scripts/ingest/jc_topic.py scripts/ingest/jc_load.py` → 0 违规；
    `wc -l` 三个文件（**97→≤100 / 44→≤60 / 66→≤80**）；三个文件各净增多少行列出来（**合计必须 ≤14**）。
 T5 **回归**：`source ~/.venvs/league/bin/activate && PYTHONPATH=.:scripts python -m pytest tests -q` → **`0 failed`**（基线 435+，按实测贴）；
    `python3 ~/bin/jc-fk-audit.py fact` → exit=0；`python3 ~/bin/jc-false-gaps.py` → **判伪 0 条**。
【铁律】① 禁 git；② 禁 print；③ ≤100/≤50/≤120；④ 只改 B1/B2/B3 三处；⑤ 报告 `.omp-logs/P0-COLLECT3b3.report.txt`（T0~T5 原始输出）
 + 全绿才 `echo TASK_DONE > .omp-logs/P0-COLLECT3b3.done`
【边界】T1 的 `gaps` 只要 **>A2** ⇒ 立刻停下（这就是"一天一趟 vs 十分钟一趟"的坑，别用"加过滤条件"绕，按 B1 的 OPTIONAL 做）；
 净增 >14 行 ⇒ 停下贴 `wc -l`；`fact.jc_odds_history` 最终不为 0 ⇒ 停下（说明 T3 没清干净）。
【验收（队长逐字复跑）】
  bash ~/bin/jc-ingest-run.sh        # gaps 必须与 T0 相同
  sudo -n -u postgres psql -d league -Atc "select count(*) from fact.jc_odds_history"   # 必须 0
  LEAGUE_FROZEN_RE="scripts/ingest/jc_write\.py|docs/db/" bash ~/bin/league-accept.sh P0-COLLECT3b3 scripts/ingest

【🎯 15:07 追加：**照抄级补丁**（队长把三处改法逐字写出来，你只需要落地 + 跑判据；前两轮都卡在"想怎么接"上，别再想了）
 ——**净增合计 8 行**（≤14 预算之内），**除下面 5 处外一个字节都不许多改**：
 ① `scripts/ingest/jc_topic.py` 第 5 行 import 区加一行：
     `from ingest import jc_odds_write`
 ② `scripts/ingest/jc_topic.py` 里 `load_topic()` 的 `else:`（现在是 `logger.info("skip %s n=%d (2e 范围)"…)`）**之前**插入 **4 行**：
     `    elif topic == "jc_odds_history":`
     `        for env in lines:`
     `            env["src_file"] = rel`
     `            ups += jc_odds_write.upsert_from_env(cur, env)`
     （`rel` 与 `lines` 都是函数里现成的变量；`upsert_from_env(cur, env) -> int` 已验收在 `jc_odds_write.py:44`，
      它从 `env["src_file"]` 取来源文件 ⇒ 所以第 ③ 行那句赋值**必须**有，别省。）
 ③ `scripts/ingest/jc_load.py` 在 `TOPICS = (...)` 之后加 **1 行**：
     `OPTIONAL = ("jc_odds_history",)`
     并把 `files_for_batch(root, marker, TOPICS)` 改成 `files_for_batch(root, marker, TOPICS + OPTIONAL)`（**同一行内改，不加行**）。
 ④ `scripts/ingest/jc_load.py` 的 `for path, state in cands or [(None, "missing")]:` 改成（**同一行内改，不加行**）：
     `for path, state in cands or ([] if topic in OPTIONAL else [(None, "missing")]):`
 ⑤ `scripts/ingest/jc_read.py` 的 `_from_manifest()` 里，`for topic in topics:` 循环体**第一句**位置加 **1 行**：
     `        if not entries and topic in OPTIONAL:`
     `            continue`
     ⚠️ ⑤ 会让 `jc_read.py` **99 → 101 行 ⇒ 直接撞 §2-D7 红线**！所以 ⑤ 写成**一行**：
     `        if not entries and topic in OPTIONAL: continue`  （**99 → 100 行，正好压在红线上，一行都不许多**）
     并且 `files_for_batch` / `_from_manifest` 的签名**不加参数**：`OPTIONAL` 从 `jc_load` 传进来的方式是——
     `from ingest.jc_load import OPTIONAL` **会循环 import** ⇒ 所以 `OPTIONAL` 这个**元组常量要在 `jc_read.py` 里自己定义一份**（`OPTIONAL = ("jc_odds_history",)`），
     `jc_load.py` ③ 里改成 `TOPICS + jc_read.OPTIONAL`（或 `from ingest.jc_read import OPTIONAL`）——**只允许一份定义，别两边各写一份**（以后加 topic 会漂移）。
【判据重申（就三条，别的都别自己加戏）】
 · **R1 现状零变化**：`bash ~/bin/jc-ingest-run.sh` ⇒ `rc=0`、`gaps` 跑前跑后**同一个数**（现在真包清单里没有 `jc_odds_history` ⇒ 一行都不该多出缺档）、`errors=[]`；
 · **R2 列了才处理**：`/tmp/q3/` 里造 `jc_odds_history/` + 一份把该 topic 列进去的 `.done`（数据文件直接 cp 那两份 `odds_history_sample/*.jsonl`），
   跑 `--dir /tmp/q3 --batch <你的 marker 名>` ⇒ `fact.jc_odds_history` **出现行数 = 34**（我 14:52 独立复算值），**测完你自己 `delete` 干净**（`ing` 角色有 DELETE 权限）；
   再跑一次**同一批** ⇒ 仍 **34**（幂等 upsert，不许变 68）；
 · **R3 全绿**：`PYTHONPATH=.:scripts python -m pytest tests -q` ⇒ **0 failed**（passed 不少于开工基线，两个数都贴）+ `omp-ast-check` 三个文件 **0 违规** + `wc -l` 三个文件全 ≤100。