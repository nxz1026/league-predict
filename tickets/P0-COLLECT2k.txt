【项目】league-v2（§2-D7 ≤100 行/文件；§9：报告用锚点、禁 git、禁 print）
【这一单只改两行，然后把真包跑通并交证据】上一单（P0-COLLECT2j）已修完 `_stamp`（T3 5/5 PASS）与 `jc_write.py:39` 折行（T6 0 违规 / 428 绿），
 并**用只读探针取证出两处它权限之外、我预先没抓到的缺陷 E1/E2**（报告 `.omp-logs/P0-COLLECT2j.report.txt` 第 54-78 行）。
 **本单就是放行这两行** + 跑 T1/T2/T4/T5。**除此之外一个字都不许动。**
【🅣 禁止阅读其它源码】只许读 `scripts/ingest/jc_write.py`（100 行）。开单第 1 分钟就 write/edit。

【E1｜`jc_write.py:72`（真包实测 `TypeError: fromisoformat: argument must be str`）】
 现状：`"business_date": date.fromisoformat(row["business_date"]) if row.get("business_date") else None,`
 取证事实：解析层 `clock()` **已经**把它换成 `datetime.date(2026, 9, 15)` 对象（工人用只读探针验过 type=date）⇒ 写层再 `fromisoformat` 必炸。
 **改成**：`"business_date": row.get("business_date"),`
 （**不许**改成 `str(...)` 再解析、**不许**加 try、**不许**动 `if row.get(...)` 之外的其它键。若 `from datetime import date` 因此变成未使用，
   请连同第 71 行 `data.update({...})` 那一段一起保持 100 行不变：**不许删 import 行以外的任何东西**；删了 import 里的 `date` 会让文件变 100 行以下，那也行，别硬撑。）
【E2｜`jc_write.py:57`（真包实测 PG `syntax error at or near ","`）】
 现状：`sql.SQL(", ").join(map(sql.Identifier, updates)) if updates else sql.Literal("NULL")`
 取证事实：`updates` 里每一项是**完整赋值语句文本** `"match_num_str = EXCLUDED.match_num_str"`，用 `sql.Identifier` 一包就变成
   `SET "match_num_str = EXCLUDED.match_num_str", ...`（整句被当成一个带引号的标识符）⇒ PG 语法错误。
 **改成**：`sql.SQL(", ").join(sql.SQL(u) for u in updates) if updates else sql.SQL("src_hash = EXCLUDED.src_hash")`
 （列名全是小写无引号，`sql.SQL` 直拼是安全的；**空 updates 分支不许留 `'NULL'` 字符串**——那是把 SET 写成值的另一个雷。）
【不许做】改 E1/E2 之外的任何逻辑；动 `jc_load.py`（**它的调用点保持原样，不要再套 `_adaptd`**）；动 `jc_read.py`；加/删测试用例；碰 DDL；真联网。

【必须成立】
 T1 `cd /home/ubuntu/league-v2/repo && source ~/.venvs/league/bin/activate && PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /tmp/incoming`
    最后一行 count 必须正好 `jc_match=17 jc_offer=170 jc_result=15 jc_issue=0 jc_issue_draw=0 jc_issue_match=0 jc_issue_prize=0 lottery_draw=0`
    且**三批都不许出现 `errors=[...]`**（有 error 就是没修好）
 T2 同命令**第二遍**：count 一字不变，且仍无 errors（两遍原始输出都要贴进报告）
 T3 `python3 ~/bin/omp-ast-check.py scripts/ingest/jc_write.py scripts/ingest/jc_load.py scripts/ingest/jc_read.py` → **0 违规**（`jc_write.py` 必须 ≤100 行）
 T4 `sudo -n -u postgres psql -d league -Atc "select count(*)||'/'||count(distinct match_id)||'/'||count(distinct snap_ts) from fact.jc_offer"`
    → **正好 `170/17/2`**（`snap_ts` 是文本列，`count(distinct)` 直接可用）
 T5 `sudo -n -u postgres psql -d league -c "select topic,src_file,rows,done_marker from ops.file_arrival order by topic,src_file"` 全表输出，
    并且 **`#MISSING` gap 行必须正好 5 条**：批 `2026-09-15T15-03-45Z` 缺 5 个 topic、批 `2026-09-15T15-16-32Z` 缺 3 个 ——
    ⚠️ 上一单报告说"0 条是因为全事务 rollback 连 ops 一起回退"，本单修好后**必须真的落库**（`ops.file_arrival` 写入与 fact 在同一事务，成功即留痕）。
 T6 `PYTHONPATH=scripts python -m pytest tests -q` → **428 passed / 0 failed**
【铁律】① 禁 git；② 禁 print；③ ≤100 行/文件、函数 ≤50 行、列 ≤120；④ stdlib + psycopg；
 ⑤ 只许改 `scripts/ingest/jc_write.py` 这一个文件的两行；
 ⑥ 报告 `.omp-logs/P0-COLLECT2k.report.txt`（T1~T6 原始输出）+ **只有 T1/T2 真绿**才写 `echo TASK_DONE > .omp-logs/P0-COLLECT2k.done`
【边界】还跑不出 17/170/15，就照上一单的规矩：**贴 count 原样 + 你用只读探针取到的取证 + 停下**，不要改别人的代码来绕。
【验收（队长逐字复跑）】
  bash ~/bin/v2b-check.sh
  bash ~/bin/league-accept.sh P0-COLLECT2k scripts/ingest
