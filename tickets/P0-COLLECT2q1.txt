【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print、不许放宽红线）
【这一单只做一件事】**`.done` 清单丢了 `topic/` 前缀时，按目录消歧把数据认回来**。
 上一版 `P0-COLLECT2q`（v1/v2）还带了 orphan，两次 attempt 共 27 分钟**零写入**（v1 12 分钟、v2 14 分钟都在读文件与思考）⇒ 队长把单**拆小**：
 **orphan 拆到 `P0-COLLECT2q2`，本单不碰 orphan**（今天没有数据因缺 orphan 而丢，那三个 22:48/22:49/22:54 文件早在窗口时代就已入库）。
【🅣 只准改 1 个文件】`scripts/ingest/jc_read.py`（现 **83 行**，改完必须 **≤100 行**）。**第 1 个动作就 write/edit，不要通读全仓。**

【A. 现场事实（照抄，别去查）】
 · 国内机从 **`2026-09-16T01-13-20Z` 批**开始把 `.done` 第一列写坏了：**丢了目录前缀**——
   ```
   01-03-20Z.done : "jczq_offer/2026-09-16T01-03Z__001.jsonl\t40\t<hash>"   ← 正常
   01-13-20Z.done : "2026-09-16T01-13Z__001.jsonl\t40\t<hash>"              ← 没有 "jczq_offer/"
   01-23-18Z / 01-30-20Z：同样坏（截至队长 09:30 共 3 批）
   ```
 · 真文件都在，且**声明行数与磁盘实际非空行数完全吻合**（队长 09:44 逐 topic 实测三批都是）：
   `jczq_offer 40 · jczq_result 9 · jclq_result 23 · jc_issue 4 · jc_issue_result 3 · lottery_draw 120 · jclq_offer 是 .empty(0)`
   ⇒ **同一分钟戳的文件名在 7 个 topic 目录里各有一份、行数互不相同** ⇒ "存在 + 行数吻合"**必然唯一命中**（这是本单敢做消歧的实测依据）。
 · 现状后果（我方管）：2o 的清单优先 ⇒ 这 3 批 7 个 topic **全部 `rejected`**、该批 **`ups=0`**、`fact.jc_offer.max(snap_ts)` 停在 `01:03:02Z`、
   并留下 **21 条假 `#MISSING`**（3 批 × 7）⇒ 消歧成功后**这 21 条不再新增**（历史假行由队长删，你不要删库）。
 · 所有 `.done` 行尾是 **CRLF** ⇒ 取字段一律 `.strip()`；第三列 hash **本单不校验**（算法未定义）。
【B. 规则（**三步，照实现，别加第四条**）】
 1) `_manifest()`：`rel` **含** `/` ⇒ 现状不变；`rel` **不含** `/` ⇒ **不要**用它的首段当 topic（那就是今天的 bug），
    改放 `out.setdefault("", []).append((rel, decl))`。
 2) 新函数 `_resolve(root, topics, rel, decl)`：在所有 topic 目录里找 `root/<t>/<rel>` 中 **`is_file()` 且 `_count_lines() == decl`** 的命中；
    · **恰好 1 个** ⇒ 返回它，并 `logger.warning("manifest-无目录前缀 topic=%s 文件=%s（按目录消歧）", t, rel)`（**这条日志必须有，是对方违约的凭证**）；
    · **0 个或 ≥2 个** ⇒ 返回 `None`（**不许猜、不许取第一个**），并 `logger.warning("manifest-无法消歧 rel=%s 声明=%d", rel, decl)`。
 3) `_from_manifest()`：消歧命中的 ⇒ 并入该 topic 的列表（`.empty` 结尾 ⇒ state 自然是 `"empty"`，**不算 gap**）；
    未命中的 ⇒ **不放进返回值**，并且**对"磁盘上确实有这个文件"的那些 topic 不记 `missing`**（改成上面那条 `无法消歧` 日志）——
    只有"清单没有、消歧没有、磁盘也没有"才允许 `[(None,"missing")]`。
【C. 不许做】改 `jc_load.py` / `jc_write.py` / 任何 DDL / 夹具；做 orphan（下一单）；删/改库里任何行；往 `/srv/league-staging/**` 写任何东西（**只读**）；
 改 2o 已落地的 `_check_entry` 行数对账语义；禁 git；禁 print；演练一律在 `/tmp/…`。
```python
# D. 骨架（照抄进 jc_read.py；总长 ≤100 行 ⇒ 允许把 _window 压行，但不许改它的语义）
def _resolve(root: Path, topics: tuple[str, ...], rel: str, decl: int) -> tuple[str, Path] | None:
    hits = [(t, root / t / rel) for t in topics
            if (root / t / rel).is_file() and _count_lines(root / t / rel) == decl]
    if len(hits) != 1:
        logger.warning("manifest-无法消歧 rel=%s 声明=%d 命中=%d", rel, decl, len(hits))
        return None
    logger.warning("manifest-无目录前缀 topic=%s 文件=%s（按目录消歧）", hits[0][0], rel)
    return hits[0]
# _manifest: rel 含 "/" 走原路；不含 "/" 进 out[""]
# _from_manifest(root, marker, topics)：先把带前缀的按现状处理；再遍历 out[""] 调 _resolve 并入；
#                                        未并入的 rel ⇒ 记 unresolved_rels，最后判 missing 时跳过"磁盘有该文件"的 topic
```
【E. 必须成立（数字来自队长 09:44 实测）】
 E1 **老三批不许变**（窗口分支回归）：`PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /tmp/incoming`
    → `ups=85 / 85 / 15`、每批 `errors=[]`
 E2 **2o 的两批也不许变**：`PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /tmp/incoming2` → `40 / 40`、`errors=[]`
 E3 **本单的存在理由**：`PYTHONPATH=scripts python scripts/ingest/jc_load.py --dir /srv/league-staging/incoming/cn-collector`
    → `2026-09-16T01-13-20Z` / `01-23-18Z` / `01-30-20Z` 三行**必须从 `ups=0` 变成 `ups=49`**（40 盘口 + 9 赛果；篮彩与传统足彩那三 topic 走 `skip` 行）；
    并且 `select max(snap_ts) from fact.jc_offer` **必须 >= `2026-09-16 01:30:02+00`**（三批的水都进来了）；
    `select count(*) from fact.jc_offer` 相对跑前**只增不减**（把跑前跑后两个数都贴出来）
 E4 **违约凭证**：上面这一趟日志里 `manifest-无目录前缀` 必须出现 **≥ 21 次**（3 批 × 7 条清单行）；贴前 8 条原文
 E5 **假 gap 不再新增**：`select count(*) from ops.file_arrival where src_file like '%#MISSING%'` 跑前跑后各贴一次 ⇒ **改后 ≤ 改前**（消歧成功的批不该再产 gap）
 E6 静态：`python3 ~/bin/omp-ast-check.py scripts/ingest/jc_read.py scripts/ingest/jc_load.py scripts/ingest/jc_write.py` → **0 违规**，并贴 `wc -l scripts/ingest/jc_read.py`（**≤100**）
 E7 `python3 ~/bin/jc-fk-audit.py fact` → exit=0
 E8 `source ~/.venvs/league/bin/activate && PYTHONPATH=scripts python -m pytest tests -q` → **428 passed / 0 failed**
【铁律】① 禁 git；② 禁 print；③ ≤100/≤50/≤120；④ **只改 `jc_read.py` 一个文件**；
 ⑤ 报告 `.omp-logs/P0-COLLECT2q.report.txt`（E1~E8 原始命令 + 原始输出 + `jc_read.py` 实际行数）
 + 全绿才 `echo TASK_DONE > .omp-logs/P0-COLLECT2q.done`
【边界】`jc_read.py` 装不进 100 行 ⇒ **贴 `wc -l` 停下**（我来决定挪 `_window`，**别自己放宽红线、别删功能**）；
 E3 的 `ups` 不是 49 ⇒ 贴那三行原始日志停下；E4 一次都没出现 ⇒ 说明你没走消歧分支，停下。
【验收（队长逐字复跑）】
  bash ~/bin/jc-ingest-run.sh
  LEAGUE_FROZEN_RE="scripts/core/|scripts/ingest/jc_load.py" bash ~/bin/league-accept.sh P0-COLLECT2q scripts/ingest/jc_read.py
