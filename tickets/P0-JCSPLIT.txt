【项目】league-v2（§2-D7 ≤100 行/文件、≤50 行/函数、≤120 列 硬红线；§9：报告用锚点、禁 git、禁 print）
【这一单**只做一件事：纯搬家**（零行为变化）——把 `scripts/ingest/jc_load.py`（**99 行**）里的 `_ops()` 与 `load_topic()` **整段搬进新文件** `scripts/ingest/jc_topic.py`】
 为什么现在必须搬：`P0-COLLECT3b3`（盘口全历史接线）要在 `jc_load.py` 里加 `WRITE` 一项 + 分支 ⇒ 99 行的文件**装不下**；
 而 `jc_load.py` 的"每 topic 怎么落库"本来就与"批次怎么扫"是两件事 ⇒ **搬完 `jc_load.py` 约 65 行、`jc_topic.py` 约 45 行**，两边都留出余量。
 【🅣 第 1 个动作就 edit，改完之前一行测试都不许跑（§9-66②）；基线我量好了，别自己测（§9-69）。】
【A. 基线（队长 11:59 实测，照抄）】`jc_load.py` **99 行**；`gaps=14`；`fact.jc_offer=1440`、`jc_match=36`、`jc_result=24`、`ops.file_arrival` 里 `done_marker=false 且 src_file not like '%#MISSING%'` = **14**（orphan 已兜住）；
 `--dir /tmp/incoming` → `ups=15`；`--dir /tmp/incoming2` → `ups=40`；`pytest tests -q` → **428 passed**。
【B. 搬家规则（**一行逻辑都不许多改**）】
 · 搬 `_ops` 与 `load_topic` **两个函数原样移动**（连注释、连 docstring、连参数默认值都不许多改一个字）；
 · 新文件 `scripts/ingest/jc_topic.py`：需要的 import 从 `jc_load.py` **一起搬过去**（`logger` / `Json` / `Path` / `jc_write` / `read_lines` / `parse_line`），
   `jc_load.py` 反过来 `from ingest.jc_topic import load_topic`（**调用点只有 `load_batch` 里那一处**）；
 · ⚠️ **不许出现循环 import**：`jc_topic.py` 里**绝对不许** `import jc_load`；`TOPICS/WRITE` 这两个常量**留在 `jc_load.py`**，
   由 `load_topic` 的**参数**收（现状本来就是收 `topic` 参数、自己判 `WRITE`？——**它现在是直接引用模块常量 `WRITE`**！
   ⇒ 所以把 `WRITE` 也**搬进 `jc_topic.py`**（`jc_load.py` 不再引用它；`TOPICS` 留在 `jc_load.py` 不动）；
 · 搬完 `wc -l`：`jc_load.py` **≤70**、`jc_topic.py` **≤60**。做不到 ⇒ **停下贴 `wc -l`**（别删注释硬挤）。
【C. 证明"纯搬家"的手段（**必须贴进报告，两向都要**）】
 C1 `cp scripts/ingest/jc_load.py /tmp/jc_load.presplit.py`（**动手之前**做）
 C2 `python3 ~/bin/jc-move-check.py /tmp/jc_load.presplit.py scripts/ingest/jc_load.py scripts/ingest/jc_topic.py` → **exit=0**
    （工具会逐函数比对 AST 源码，打印 `MATCHED <函数> -> <文件>` / `--- OK: 纯机械搬移`；**有逻辑改动就会 FAIL**）
 C3 **反证**：`LEAGUE_ALLOW_ADD= python3 ~/bin/jc-move-check.py /tmp/jc_load.presplit.py scripts/ingest/jc_load.py scripts/ingest/jc_topic.py` → 同样 **exit=0**
    （若这一步 FAIL ⇒ 说明你有新增行，纯搬家不该有；把它退回零新增再来）
【D. 不许做】改 `_ops`/`load_topic` 内部任何一行逻辑（连"顺手加个日志"都不许）；改 `jc_read.py`(97)/`jc_write.py`(100,md5 锁)/解析器/DDL/夹具；
 删或改库里任何一行；往 `/srv/league-staging/**` 写；禁 git；禁 print；演练一律 `/tmp/…`。
【E. 必须成立（改完之后按序跑，原始输出全进报告）】
 E1 `bash ~/bin/jc-ingest-run.sh` → `rc=0`、每批 `errors=[]`、`orphan 装载 n=0`（已兜过 ⇒ **必须还是 0**，别把孤儿当新孤儿再装一遍）
 E2 `sudo -n -u postgres psql -d league -Atc "select count(*) from fact.jc_offer"` = **1440**、`… jc_match` = **36**、`… jc_result` = **24**
     以及 `… "select count(*) from ops.file_arrival where src_file like '%#MISSING%'"` = **14**（**搬家不许动账本**）
 E3 `--dir /tmp/incoming` → `ups=15`、`--dir /tmp/incoming2` → `ups=40`、`errors=[]`（与基线一字不差）
 E4 `python3 ~/bin/omp-ast-check.py scripts/ingest/jc_load.py scripts/ingest/jc_topic.py` → 0 违规；`wc -l` 两个文件
 E5 `python3 ~/bin/jc-fk-audit.py fact` → exit=0；`source ~/.venvs/league/bin/activate && PYTHONPATH=scripts python -m pytest tests -q` → **428 passed / 0 failed**
【铁律】① 禁 git；② 禁 print；③ ≤100/≤50/≤120；④ **只搬不改**；⑤ 报告 `.omp-logs/P0-JCSPLIT.report.txt`（B/C/E 全部原始输出）
 + 全绿才 `echo TASK_DONE > .omp-logs/P0-JCSPLIT.done`
【边界】C2/C3 任一不 exit=0 ⇒ **停下贴原文**（那是你偷偷改了逻辑）；E2 任何数变了 ⇒ **停下贴原文**（搬家不许碰数据）；超行 ⇒ 停下贴 `wc -l`。
【验收（队长逐字复跑）】
  python3 ~/bin/jc-move-check.py /tmp/jc_load.presplit.py scripts/ingest/jc_load.py scripts/ingest/jc_topic.py
  bash ~/bin/jc-ingest-run.sh
  LEAGUE_FROZEN_RE="scripts/core/|scripts/ingest/jc_(read|write)\.py" bash ~/bin/league-accept.sh P0-JCSPLIT scripts/ingest
