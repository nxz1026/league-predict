【项目】league-v2（教训同源：你自己上一单 P0-BACKFILL1c 报告 §7 列出的"同类依赖真库存量"隐患清单——本单就是把那份清单**落到代码**）
【任务】只改 1 个文件：`tests/test_backfill_transport.py`（≤100 行）
  · 把 A1~A3（你报告里点名的 3 处）从"读真 `ops.quota_ledger` 当日余额"改成**假账本**：
    沿用你在 `test_backfill_plan.py` 里已经写好的 `tap` 思路（`monkeypatch.setattr(quota, "remaining", …)` / `charge`），
    余额由用例自己给常量；**删掉所有 `if remaining < 1: pytest.skip(...)`**（静默 skip 比失败更坏：它让门形同虚设）。
  · 顺带（同文件内）：凡断言里出现"与真库当前值比较"的地方（`before - n` 这类）改成**与常量比较**；
    真库那部分语义（`charge` 的 SQL 守卫真能原子拦下越界）已由 `tests/test_quota.py` 覆盖，本单**不必**在这里重复。
【必须成立】
  · `python -m pytest tests -q` 终值仍是 **305 passed、零 FAILED、零 SKIPPED**（用 `pytest -q -rs` 复核没有 skip 残留）；快档 **231 passed**。
  · **代理绊线自证零联网**：`https_proxy=http://127.0.0.1:9 http_proxy=http://127.0.0.1:9 python -m pytest tests/test_backfill_transport.py -q` 必须全绿。
  · **配额耗尽场景必须真的被执行到**（原来靠"今天恰好余额为 0"，现在由假账本固定造出 `remaining=0`）：断言 `QuotaExceeded` 抛出且 `urlopen` 调用数为 0。
【铁律】① 禁 git；② 只许改这一个测试文件，**`scripts/**` 生产码零改动**（发现生产 bug 就写进报告，别顺手改）；
  ③ ≤100 行、AST 函数 ≤50 行、单行 ≤120 列，自检 0 违规：`python3 ~/bin/omp-ast-check.py tests/test_backfill_transport.py`；
  ④ **不许删用例、不许放宽断言**（宁可报数不符也别动数字，改完总数仍是 305）；
  ⑤ 禁真联网；`~/bin/omp-ast-check.py`、`~/bin/league-accept.sh` 只读禁改；
  ⑥ 完成后写 `.omp-logs/P0-BACKFILL1d.report.txt`（改前/改后对照 + `-rs` 原始输出证明零 skip + 绊线输出），然后 `echo TASK_DONE > .omp-logs/P0-BACKFILL1d.done`
【验收（队长逐字复跑）】
  bash ~/bin/league-accept.sh P0-BACKFILL1d tests/test_backfill_transport.py
  cd ~/league-v2/repo && source ~/.venvs/league/bin/activate && python -m pytest tests -q -rs 2>&1 | tail -3   # 期望 305 passed，无 SKIPPED 段
  # 队长另做：grep 确认该文件里已无 quota.remaining( 真调用与 pytest.skip，两条通道都封死
