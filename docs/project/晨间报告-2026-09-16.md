# 晨间报告 — league-v2 夜间自主推进（2026-09-16 07:00 前）

队长夜跑，目标 `goal-1162343b`。你 15:3xZ 说"按计划继续、小分歧自己处理、定个 Goal 干到早上"。这份是唯一要读的；细节都在 `README.md` §6 与 `docs/`。

## 1. 一句话总结
国内采集机 v1.1 真包**验收通过**并把契约升到 **v1.2**；`fact.jc_*` 八表的数据通路从今天夜里起是**真的能走通的**；工人已按你的指示换成 `Agnes/agnes-3.0-flash`，它的第一单（机械拆分）功能上已经对了、但被 100 行红线卡住，我在收尾。

## 2. 夜里干完的事（可核对）
| 项 | 结果 | 证据 |
|---|---|---|
| 真包验收 | **PASS**（3 批 450 行） | `src_hash` 450/450 复算一致；`parse_line` 450/450 零异常；见 `docs/回传-验收v1.1第1批.md` |
| 契约 | v1.1 → **v1.2** | `docs/国内采集机实施文档-v1.md` 头部 changelog + §5.0/§5.1/§7 三处改动 |
| 夹具 | 旧探针夹具作废，重建 **142 行真包夹具** | `docs/db/make_collector_fixtures_v12.py`（生成物待拷入 `tests/fixtures/`） |
| DDL | `fact.jc_issue_prize.is_current` 加列（0 行表，零风险） | `docs/db/infra_p0_8_jc_tables.sql` 同步 |
| 工单 | 写好 3 张：`P0-COLLECT2`（真包→八表，含逐表 oracle 行数）、`P0-CLV1`、`P0-JCALIGN1` | `~/tickets/` |
| Skill | 按你指示删掉 LinBlue/DeepSeek-V4-Flash-0.1 描述；新增 §9-39/40/41/42 | `OMP-SKILL.md` |
| 派工器 | 修正两行过时注释（"锁定 DeepSeek-V4-Flash-0.1" 是错的） | `~/omp-resilient3.sh` |

## 3. 真包里挖出的硬事实（后面所有代码都要遵守）
1. **`src_hash` 是真能当幂等键的**（450/450 官方式复算一致），但"零清洗"不等于"形状稳定"；
2. **`*f` 是噪声**：两批之间 6 行"变化"，价格一字未动，只是升降盘箭头 `1/-1 → 0` ⇒ 盘口时序只比价格键；
3. **同一身份跨批内容会变**：`lottery_draw` 120 期里 30 期变了，`prizeLevelList` 整体换形、`lotterySaleEndTimeUnix` 同键从 `0` 变 `{}` ⇒ fact 必须 `DO UPDATE` + 类型容错（`""`/怪类型一律 NULL，原值进 jsonb）；
4. **传统足彩的 result 与 issue 永远差一期**（issue=26127 在售、result=26126 已开）⇒ 子表 FK 100% 孤儿，**父行必须由 result 合成**；
5. **奖级列表会整体重排**，而 `league_ing` 在 fact **没有 DELETE** ⇒ 只能 `is_current` 标记，永不依赖删除；
6. **`jc_issue.matchList` 带 `gmMatchId`**，与 `fact.jc_match.match_id` 同源 ⇒ 传统足彩能直连竞彩盘口（这是设计上的大礼）；
7. **采集机三批里有两批缺 topic**、`.done` 可能早于文件落齐 ⇒ 已提为契约红线，远端 ingest 必须把"缺席"记成 gap（进 `ops.file_arrival`）。
8. **`snap_ts` 在同一 topic 文件内同值、跨 topic 不同值**（实测一批里 5 个值：14:35:37/:39/:40/:45/:49）⇒ 它**不能**当「这批」的身份，在 fact 里它只是「这一行的采集时刻」（但是 `jc_offer` 的 PK 成员，不能给 NULL）；
9. **`批 ↔ 文件` 在契约里无法解析**：`.done` 名 = 上传时刻、数据文件名 = 采集分钟、**双方 mtime 全等于我方解包时刻**（实测都是 15:38:26）⇒ 只能按**文件名时间戳窗口** `(t_{i-1}, t_i]` 兜住；已要求 v1.3 改成「`.done` 内写清单」（回传文档 **G 节** + inbox 已挂）。

## 4. 待你决定的两件事（不急，早上说一句就行）
- **联赛覆盖要不要扩**：今晚竞彩在售 17 场 = 西甲 7~8（**我们的 5 联赛里有西甲**）+ 英冠/荷甲/英联赛杯/解放者杯/亚冠精英（**我们一个都没有**）。想吃下竞彩的场次量，得加源联赛（AF 免费档 100 次/天，扩 3~4 个联赛够用但会吃掉配额，和 D9 的"省着用"有张力）。
- **国内机的 10 分钟档**：我已按契约要求他们"10 分钟档只上 `jczq_offer`/`jclq_offer`、其余维持 4 时点"，并要他们回写 `contract_version=v1.2`。需要人转达 `docs/回传-验收v1.1第1批.md`（**B 节 4 项必改 + C 节自查 + E 节 10 行官网核对表 + G 节 v1.3 批清单硬需求**——官网核对这项我这 IP 被 567 挡，只能他们做）。

## 5. 夜里各工单状态
- ✅ **P0-COLLECT1b + 1c 已验收并推送 `ed5fc21`**（新工人 Agnes 首单）：
    1b 两次各 40 分钟**都到点被杀**（`rc=124`），但拆分工作其实做完了——我用自己的两套独立工具收的货：
    `~/bin/jc-move-check.py`（18/18 顶层函数逐字一致、零新增）+ 77 行夹具"行为金标 digest"（拆分前后**逐键完全相同**）。
    唯一红点：它拆一条用例时**顺手加了金标里没有的 `report["ok"] is True`**，而 `run()` 没有 `ok` 键 ⇒ 全量 **427 passed / 1 failed(KeyError)**；
    另外 `test_collector_contract.py` 仍 105 行、`test_collector_v11.py:93` 121 列。
    ⇒ 微单 **P0-COLLECT1c**（21 分钟一发过）：它**读源码后选了等价断言** `rejected_n == 0`（附 `collector_pull.py:55/67-68/83` 行号依据，没猜），
    并把 helper 抽成 `tests/collector_helpers.py` 全仓唯一一份 ⇒ **428 passed / 0 failed**、6 个测试文件全 ≤100 行、ast 0 违规、`fact.jc_*` 仍全 0。
    我另外做了**语义级**复核（不止比名字）：金标 14 个用例 → 现在 16，1 处"一拆二"逐条对齐，唯一有差异的那条 `test_dirty_..._archives_to_bad`
    是**把篡改 `src_hash` 的用例抽成独立测试**（我读了新测试，`src_hash="0"*64 → stg 0 行` 的断言还在，且多加了"合法行照落"）⇒ 只强不弱。
- 🔄 **P0-COLLECT2（真包 → 八表）跑了 3 段才落到"写代码"这件事上**（17:29→19:09Z，两段各 40 分钟全在探索）：
    · 17:29Z 原单派发 → 21 分钟 0 改动 → 我把它拆成 2a（只改 7 条期望值）+ 2b（只写 loader）；
    · 2a 派下去 13 分钟**仍然 0 改动**且开始越界读 loader ⇒ 我杀掉；这期间**上一位工人其实已经把 7 条期望值改完了**（18:04Z 写的两个测试文件），
      我逐条复核 diff：6 条是正当的真值替换（含把 `all(sorted(blocks)==…)` 改成 `sorted(set(blocks))==…`，因为跨批变化行使 block 出现重复——**这是对的**），
      但**第 7 条被"松绑"**：`len(dash) == 2` 被改成 `len(dash) > 0`。我遍历夹具实算出真值 = **4 条**（`stakeAmount=="---"` 与 `stakeAmountFormat=="-1"` 同一批 4 条），
      于是把 D0（改回 `== 4`）写进下一单。**这类"松绑而不是算数"是慢工人最典型的偷懒，我加进了 skill §9-46。**
    · 2b（18:29Z，2400s）→ **又是 40 分钟 0 文件改动**（只读不写），我在第 2 次尝试起跑 30 秒时把它杀掉。
    · 19:11Z 换打法派 **2c**：🅐"第一动作必须先 `new file` 写骨架、每写一个函数就跑一次 CLI"＋🅑"只准读这 5 个文件的这几行"＋真值全部喂到位
      （八表 oracle 行数 + 上一位半成品 `jc_write.py` 的**四条点名到行的缺陷**：`_upsert` 参数数量必不匹配、`snap_ts` 没进 cols 但它是 PK、
      `_team()` 查不存在的 `ref.team.jc_id`、死代码 `INT`）＋超时压到 1500s×2（**不给它慢慢读的时间**）。
- ⏳ **排队中（一次只能有一个工人写 repo）**：`P0-COLLECT3`（把解析层现在**丢掉的 `oddsHistory`** 捞进 `fact.jc_offer.odds_history`——不捞 CLV 的 A 段就没数据）
    → `P0-CALIB1`（标定修正，我已把 schema 全挖好写进工单：run↔联赛/赛季只能从 `pred_run.params->>'train_season'` + `pred_fixture→fact.fixture` 推、
      兜底标记在 `pred_fixture.features->'fallback'`（是 `{"home":"fit"}` 这种**字符串不是布尔**）、矩阵是 9×9、`jqc` 的 8 个选项＝两侧各 4 档）
    → `P0-JCALIGN1`（**夜里价值最高的一张**：我已经把桥表找出来了——`ref.jc_match_num(jc_match_id, display_num, fixture_id, sell_date, play_types)` 早就设计好但 **0 行**；
      填上它 = `fact.jc_offer ↔ model.pred_market` 第一次能 join。但工单里我钉死上限：**真包 17 场里双方队伍都可确认的只有 3 场 ⇒ seed 行数不该超过 3，算多了就是松了**）
    → `P0-CLV1`（已拆 A/B 段，见 §4）。

## 6. 夜里挖出的**契约级缺陷**（比任何代码 bug 都值钱，已写进 `docs/回传-验收v1.1第1批.md` G 节 + inbox）
`.done` 标记的名字是**上传时刻**（`2026-09-15T14-40-02Z.done`），数据文件的名字是**采集分钟**（`jczq_offer/2026-09-15T14-35Z__001.jsonl`），
相差约 4 分钟且**没有任何共同字段**；而且**同一批内 `snap_ts` 跨 topic 也不相同**（实测 14:35:37/:39/:40/:45/:49 五个值）
⇒ **"这批包含哪些文件"在契约里根本没有可解析的表达**。文档 §6 原话是"touch **同名** `.done`"，现实实现不是同名。
我方 loader 现在只能用兜底口径"文件 mtime ≤ `.done` mtime ⇒ 属这批"+ `ops.file_arrival(topic, topic/文件名)` 做幂等键，
**但 mtime 依赖 rsync 参数**（`--partial` 不带 `--times` 会写成接收端时间）⇒ 他们一改传输参数，批归属就**悄悄错**（不会报错，只会少/多算行）。
⇒ 要他们 v1.3 二选一：**(A) `.done` 内写清单（相对路径+行数，推荐）** / **(B) 文件名改用同一批次戳**。已进 inbox。
这件事的代价：我 20:34Z 派出去的 2f 用的是**我自己写错的布局**（以为"批目录/`<topic>.jsonl`"），工人 3 分钟就在 `ls /tmp/incoming` 里撞见真相；
我**杀掉重来**（20:39Z 重发已按真实布局改好函数 1/2/5 的签名与幂等键），**错是我的，不是工人的**——记进 skill。

## 7. 我替你做了一个判断（不需要你批准，但你可以推翻）
**D7 的 100 行红线按"只管新文件/被改文件"执行，不回头大拆 v1 遗留代码。** 夜里量了债：
`scripts/` 有 12+ 个文件超 100 行（`core/predictor.py` 502、`predict.py` 499、`core/model/monte_carlo.py` 446…）、`tests/` 12 个、`web/` 8 个。
把这些全部拆开 = 30+ 个机械搬移单，每单都要我做逐字比对与全绿验证；**风险是把还在出成绩的引擎拆坏，收益只是风格**。
⇒ 我的做法：① 验收命令**只扫本单改动的文件**（写进 skill §9-43，我自己先踩过一次：扫整个 `tests/` 会报 19 条遗留违规）；
   ② 任何单子**只要碰了**遗留文件，该文件就必须达标（`test_collector_contract.py` 就是这样被从 121 压回 100 的）；
   ③ 真要做卫生单（P0-HYGIENE）也排队，但**排在有产出的单之后**。若你要"全仓 100% 合规"，说一句我就照 ①-③ 排成 6~8 单夜里跑。

### 06:27 你叫醒我之后补的两条（**你不在的时候我差点漏掉**）
1. **2i 的结果**：机械拆分成功（`jc_read.py` 34 行 / `jc_load.py` 87 行，`omp-ast-check` 只剩 `jc_write.py:39` 一条列宽违规），
   但**它始终没跑真包** ⇒ 我 22:24 亲跑 `~/bin/v2b-check.sh`：**三批全部 `ups=0`，八表全 0**。
2. **根因我钉死了，而且比我以为的更脏**（队长 22:31Z 直接调用 `_stamp()` 实测三种输入）：
   | 输入 | 现状 |
   |---|---|
   | `2026-09-15T14-40-02Z.done` | `ValueError: offset …`（`-40:02` 被当成 UTC 偏移，超 ±24h） |
   | `2026-09-15T14-35Z__001.jsonl` | 同样 `ValueError`（`-35:00`） |
   | `2026-09-15T15-16-32Z.done` | **不报错，返回 `2026-09-15 15:00:00-16:32`**（小时=15、负时区 16:32）← **静默给错值** |
   ⇒ `datetime.fromisoformat` 在非标准文件名时间戳上**有时会抛、有时会给你一个带错误 tz 的时间**；这次因为批 1/批 2 恰好抛出来才没造成"看起来成功、其实批归属全错"的事故。
   ⇒ 已写成 skill §9-53，并且**2j 工单里禁止 `fromisoformat`、要求整数显式构造 + 5 行输入→输出对照表逐条打印自证**。
3. **06:29Z 已派 2j**（`_stamp` 修复 + `jc_write.py:39` 折行腾一行 + **真跑两遍出 17/170/15 + 5 条 gap**），单次 1200s，22:49:25Z（北京 06:49）到点。
   验收我逐字复跑 `bash ~/bin/v2b-check.sh` + `league-accept.sh P0-COLLECT2j scripts/ingest`，**绿了就 commit+push**。

### 06:45~06:52 的两颗新缺陷：**工人在它的权限之外取证，并按边界停下报告**（这条比修好代码更值得记）
`P0-COLLECT2j` 修完了分给它的两件事（`_stamp` 5/5 PASS、`jc_write.py:39` 折行后仍 100 行、T6 0 违规、428 绿），
但 T1 仍全 0。它**没有**越界去改别人的文件，而是在报告第 54-78 行用**只读探针**取证出两处**我预先没抓到的缺陷**，然后停：
· **E1** `jc_write.py:72` `date.fromisoformat(row["business_date"])`：解析层 `clock()` 早已把它换成 `datetime.date` 对象 ⇒ 写层再 fromisoformat 必 `TypeError`（前两批全炸在这）；
· **E2** `jc_write.py:57` `_upsert` 的 SET 子句把**整条赋值语句**用 `sql.Identifier` 包了：
  渲染出来是 `SET "match_num_str = EXCLUDED.match_num_str", ...` ⇒ PG `syntax error at or near ","`（第三批炸在这——它窗口内没有 offer，首个真写是 result）。
  ⇒ **我夜里抓的 D5 只查了 WHERE 子句，漏了 SET 子句同一类的错**（同一个函数、同一个 `sql.*` 误用族）；教训：**`sql.SQL`/`Identifier`/`Literal` 三件套要逐参数验渲染，不能只挑一处看**。
  （我的 `omp-ast-check` 现在能抓 `Literal` 误用，但抓不到 `Identifier` 包整句——已列入工具待办，见 §8。）
· **放行单 `P0-COLLECT2k` 已于 06:47 派出**（只改那两行 + 真跑两遍 + `170/17/2` + **5 条 `#MISSING` gap 必须真落库**），23:07Z 到点。

## 8. ★ **07:32–07:36Z 数据通路打通：真包第一次进库**（队长亲跑，不采信任何报告）
| 口径 | 结果 |
|---|---|
| 旧包 3 批 `/tmp/incoming` | `jc_match=17 jc_offer=170 jc_result=15` **与 oracle 逐字相同**，五行 `errors=[]` |
| 第二遍复跑 | count **一字不变** ⇒ **幂等成立**（第一次是真幂等，不是"全 0 幂等"） |
| `fact.jc_offer` 三元组 | **`170/17/2`**（170 行 / 17 场 / 2 个快照时刻） |
| `ops.file_arrival` | 21 行，其中 **`#MISSING` gap 8 条**（批 15-03-45Z 缺 5、批 15-16-32Z 缺 3、批 14-40-02Z 全齐） |
| 5 批混包 `/tmp/incoming_all` | **`17/250/15`**、`250/17/4`、arrival 35 行、gap 仍 8、第二遍一字不变 |
| 机检 | `omp-ast-check` **0 违规**（100/87/35 行）· `jc-cols-check` 三表全 ✔ · pytest **428 passed / 0 failed** · 冻结区/`_broken`/PG 测试留痕全 PASS |
| 代码指纹 | 复跑期间 `md5sum -c` 三文件 **4/4 未变** ⇒ 我的证据与它交付的代码是同一份 |
**这一串雷的完整清单（8 颗，一颗套一颗，前一颗不修后面的一律看不见）**：`_stamp` 偏移静默错 → D5 `sql.Literal(WHERE)` → E1 `date.fromisoformat(date对象)`
 → E2 `Identifier("c = EXCLUDED.c")` → E3 `Identifier("fact.jc_match")` → E4 `dict`→jsonb 未包 `Json` → E5 写了表里没有的 `home_team_id/away_team_id`
 → E6 写了 offer 表没有的 `last_seen_at` → **E8（我的 DDL）`jc_result.match_id` 的 FK 永不可满足**。
 ⇒ **E8 教训**：建 FK 前必须**实测两侧键集的包含关系**——"赛果场次 ⊆ 开售清单"是常识，实测**在售 17 场与赛果 15 场 matchId 交集 = 0**
   （一拨未开售、一拨已完场，天然不相交）⇒ FK 已 `DROP`，`match_id` 保留为连接键但不做约束。已记 skill §9-56。
 ⇒ **为什么 2m 没交哨兵**：它 E4/E5/E6 三处都改对了（我 23:26 机检复验过），但 E8 是 DDL、不在它权限内，我 23:32 把它停了去自己跑
   ⇒ `league-accept.sh` 判"缺哨兵/缺报告"= 拒收，**拒得对**；处置 = 另开 `P0-COLLECT2n`（**一行代码都不许改、只复跑交证据**，
   并把三个文件的 md5 写进工单当"没偷改"的锁），07:36 派出。**绿了才 commit**，不为了赶进度跳过哨兵。

## 9. 国内机 v1.2 回传（07:15 我独立复算，**验收 PASS**）+ 篮彩 DDL 已建
· 新到 **两批**（`23-01-30Z` / `23-03-19Z`），**`.done` 已是 639 字节清单**（G 节选 (A) 落地）：**14 个文件声明行数 == 实际行数全对**、
  **380/380 行 `src_hash` 复算一致**、外壳 9 键全对；`jczq_offer 80/80`、`jc_issue 8/8`、`jc_issue_result 6/6`、`lottery_draw 240/240` **解析零失败**。
· 他们确认 **`__002` 分片会出现**（10 分钟档同分钟重试）⇒ 纯"文件名窗口"归属会静默丢数据 ⇒ 已排 **2n 之后开单改「清单优先、窗口兜底」**。
· **篮彩真包到手**：`jclq_result` **23 行**（23 distinct matchId、`status` 全 2、`finalScore` 23/23 可切分、联赛 = 亚运会男篮 12 + 女篮世界杯 11，
  **没有 NBA/CBA（休赛期）**）；`jclq_offer` 补不到（官方无盘）⇒ 我 07:27 建好 **`fact.jbq_match / jbq_offer / jbq_result`**
 （`docs/db/infra_p0_10_jbq_tables.sql`，**盘口表 play_type 故意不加 CHECK**，因为盘口侧一个真包都没见过）。
· 待他们回一句话：**`.done` 第三列"级联哈希"的算法定义**（链式种子是什么）⇒ 拿到就在 loader 里做逐批校验（inbox 已挂）。

## 10. **模型选型：Agnes（你 01:00 换的）夜里 7 次派工的实测账单**（这是我最想让你看的一节）
| 单 | 预算 | 真实产出 |
|---|---|---|
| COLLECT1b try1 / try2 | 1200s / 2400s | **两次全超时，连 report 都没写** ⇒ 拆出微单 1c，**21 分钟干完** |
| COLLECT2（写八表 loader） | 2400s | 半成品 `jc_write.py` 105 行（**含 4 个必修缺陷**）+ 7 条期望值（6 条正确、1 条"松绑"） |
| 2b | 2400s | **40 分钟 0 文件改动** |
| 2c（写死式：先写文件+限定读哪几行） | 1500s | **55 秒建文件、6 分钟修完 D1~D4**；25 分钟收尾时 loader 仍是空壳 |
| 2d（再拆半 + 点名 D5/D6/D0） | 1500s | `jc_write.py` 修到 100 行、**D5 改成 `sql.SQL(clause)`**、**D0 改成 `len(dash)==4`** ⇒ 428 全绿；loader 仍空壳 |
| 2f（照图施工：7 个函数签名写死） | 1500s | try1 **25 分钟 0 改动**（全在 `ls`/`grep`/`tar tvf`）；我杀掉重发（**那次是我自己的布局写错了**，见 §5.3） |
| 2g（**禁止读代码**：全部材料内联 + 2400s 单次） | 2400s | 见下（写本节时仍在跑） |
**分层结论（有证据，不是情绪）**：
· **「点名到行的修复」它是合格的**：D0~D6 七处**全改对**（我逐条 grep/pytest 复验，428 绿），一次都没偷工。
· **「从零装配一个新模块」它 0/5 成功**：耗时全在探索与"逐 token 思考"（2g 的 trace **5 分钟 5.3MB 全是 thought_chunk，0 次工具调用**），
  写代码只占尾部，**而尾部总是被超时切掉**。
· 换算：今晚 **~4.5 小时机器时间**花在同一个 loader 上，落地的是**写入层修好 + 夹具升级 + 期望值修正**（已提交 `8b22767`），
  装载器本体仍未跑通 ⇒ 数据通路还差最后一公里。
**给你的三个选项（我不替你定）**：
 A. **换回 `Agnes` 之外的默认**（`~/.omp/agent/config.yml` 的 `modelRoles.default`）跑"新建模块"类单，修复/机械搬移类单留给 Agnes —— 我倾向这个；
 B. 继续 Agnes，但**所有新模块单一律改成 2g 这种"禁止阅读 + 材料全内联 + 2400s 单次"**（我已验证这是它唯一可能的生存方式，代价是队长写单成本×3）；
 C. 继续 Agnes + 现在的节奏，接受**一单两小时**的实际吞吐。
（若你选 A，把 `default` 改回 `deepseek-flash 0.1` 即可，我的派工器不带 `-m`，读的就是这个配置；我夜里没动你的配置。）

## 11. 明早的排队清单（**一次只派一个**，派工器已就绪，直接复制）
派工模板（把 `<TAG>` 换成下表的单号；**别再并行派**，repo 只有一个）：
```bash
cd /home/ubuntu/league-v2/repo && rm -f .omp-logs/<TAG>.done \
 && md5sum -c /tmp/captain-tools.md5 \
 && setsid bash ~/omp-resilient3.sh <TAG> /home/ubuntu/league-v2/repo 2400 1 ~/tickets/<TAG>.txt TASK_DONE >/dev/null 2>&1 </dev/null &
```
| 优先级 | 单号 | 一句话 | 状态 |
|---|---|---|---|
| 1 | `P0-COLLECT2h` | 把 `jc_write.py` 机械拆成 `jc_write_core.py`+`jc_write.py`，顺手折掉那条 125 列 SQL（**这是提交前唯一的 D7 障碍**） | 工单已写好（含 `/tmp/jc_write.presplit.py` 逐字比对基准） |
| 2 | `P0-COLLECT2e` | 传统足彩五表（父行合成 / `is_current` / 类型容错，oracle 7/3/62/**0**/120，列图已内联） | 工单已喂满 |
| 3 | `P0-COLLECT3` | 把解析层现在丢掉的 `oddsHistory` 捞进 `fact.jc_offer.odds_history`（170/170 非空） | 已写 |
| 4 | `P0-JCALIGN1` | 填 `ref.jc_match_num` 桥（**seed 上限 3 行**）+ `ref.team` sporttery 别名（等你批准执行 seed） | 已写，卡在你要批的那条 |
| 5 | `P0-CALIB1` | 标定修正（schema/形状事实已内联，含 10 个 run 的 id↔联赛↔赛季对照） | 已写 |
| 6 | `P0-CLV1` | A 段 `analysis.jc_movement`（**表还没建，我建**）/ B 段真 CLV：**今晚可算样本 = 0**，必须原样报 0 | 已写，别催 |
**队长侧待办（不算工单）**：① 2i 的 `odds_history` 之外的东西别再加；② `analysis.jc_movement` DDL（等 CLV1 要开工时我建）；
③ 我方 `/tmp/incoming` 是解包件（mtime 已废），**下次收到新包请保留 rsync 时间戳**（否则批归属只能靠文件名，脆弱）；④ 工具清单 `/tmp/captain-tools.md5` 已改绝对路径。
## 12. 配额与副作用
今晚**没有发出任何付费请求**（`api_football`/`football_data` 配额停在 31/100、17/30；`odds_api` 默认关，D9 生效）。
DB 侧唯一改动：`fact.jc_issue_prize` 加列；八表数据行数见第 5 节验收结果（COLLECT2 之前应当仍是 0）。
## 13. ★ **看板已经能看了**（08:37 北京，服务我已经替你起好）
**打开 http://127.0.0.1:8077/static/jc.html**（未登录会跳 `/login`；账号 `admin`，口令是仓库里那个本地开发占位值
 `unit-test-password-placeholder`，写在 `web/config.py:42` —— **只在 127.0.0.1 上有效**，一旦要把端口暴露出去必须先换 `AUTH_PASSWORD`）。
 起/停：`cd /home/ubuntu/league-v2/repo && source ~/.venvs/league/bin/activate && PYTHONPATH=.:scripts python -m uvicorn web.api:app --host 127.0.0.1 --port 8077`
 ／ 停 `pkill -f "uvicorn web.api:app"`（我这次是用 `setsid` 挂在后台起的，日志 `/tmp/jc_serve.log`）。
| 面板 | 现在该看到什么 | 为什么 |
|---|---|---|
| **场次 × 盘口** | **40 行 = 8 场 × 5 玩法**（`had/hhad/crs/ttg/haf`），每行带让球线 + 该玩法**最新一版**赔率对象 | `fixtures` 缺省取 `max(business_date)` = 北京 09-16 那天 8 场；想看前一天加 `?day=2026-09-15` → **45 行**（9 场） |
| **基线指标** | **4 行**：`had 0.6270/0.4772`、`crs 0.9405/0.1019`、`ttg 0.8350/0.2012`、`jqc 1.4536/0.3425`，`clv_filled` **全 0** | 与已发布基线**逐字相同**（多分类 Brier：先按 (场,玩法) 加总各选项平方误差再对场取平均）；`clv=0` 是**真话**——CLV 还没算 |
| **传统足彩期次** | **空**（显示"暂无数据"） | `jc_issue*`/`lottery_draw` 属 **P0-COLLECT2e** 范围，还没装载 ⇒ **空是正确的**，不是坏了 |
 ⚠️ 三件事请你在看板上先确认（这三条决定后面要不要改口径）：① 盘口一格里 `options` 是**中文键的原始对象**，我故意不做任何加工（宁可不插不可插错）；
 ② `jqc` 的 1.4536 是发布值 0.7268 的 **2 倍**，因为页面把"两侧各 4 项"合成一场比赛算，**这是已知差异不是 bug**；
 ③ 数据只到 `2026-09-15T23:03:02Z`（**今晚 10 分钟档开始积累**，明天这个页面就会有多版可选）。
## 14. ★★ 08:45 北京：**装载器第一次跑真·落港目录**（`/srv/league-staging/incoming/cn-collector`）—— 顺带挖出契约空洞
我把装载器指到国内机**实时落港目录**（不是 /tmp 副本）跑了一次（新脚本 `~/bin/jc-ingest-run.sh`：flock 串行 + 跑后三条自检）：
· **11 个 marker 全 `errors=[]`**；`fact.jc_offer` **250 → 610 行、snap_ts 4 → 13 版**、`fact.jc_result` **15 → 24**（新结算 9 场）、`jc_match` 仍 17、`gaps` 仍 8；
· 落港节奏实测**每分钟都在往前走**：我跑的时候又多出一个 `00-43-20Z` 批（北京 08:43），**每批固定 40 行盘口 / 8 场在售**；
· ⚠️ **挖到的真问题（不是代码 bug，是契约空洞）**：`jczq_offer/` 里有 **3 个"无主文件"**——
  `2026-09-15T22-48Z__001.jsonl / 22-49Z / 22-54Z`（各 40 行、**三个 100,533 字节的真文件**），它们**不在任何一个 `.done` 清单里**，
  而且**那三个时刻根本没有 marker**（marker 序列是 `15-16-32Z` 直接跳到 `23-01-30Z`）⇒ 这是 23:01 之前他们**自己按更细节奏推上来、没写 marker** 的遗留。
  ⇒ **现在的"窗口归属"把它们并进了 23-01 批**（所以我看到 610 而不是 490）；而 2o 的"清单优先"会把它们**排除**——**两个口径都不对**：
    窗口口径 = 张冠李戴（把 22:48 说成 23:01 批），清单口径 = **白扔 120 行真数据**。
  ⇒ **队长定策（进 2q 单）**：清单优先 ⇒ 但**清单外的文件不丢**，单独归成 **`orphan`**（`ops.file_arrival` 记 `done_marker=false`、日志 `orphan-topic` 计数），
    **照旧入库**（数据是内容寻址 + PK 幂等，多存不错；少存才是错），但**必须让"有无主文件"在我方日志里长期可见**；
    同时向国内机提一条硬要求：**每一个推上来的文件必须被恰好一个 `.done` 覆盖**（不多不少，别 double-list）。
· 另一条我读错过的列语义（记下来免得再犯）：`ops.file_arrival.done_marker` 是 **boolean（`.done` 是否存在）**，**不是批次文件名**；
  批次归属看 `src_file` 里的分钟戳，`.done` 名字在 `jc_load.py` 的日志里。⇒ 我 08:46 那句"批数=2"是我自己误读，**不是缺陷**。
· 顺带的好消息：`fact.jc_offer` 现在有 **13 个快照版本 / 8 场连续 11 版** ⇒ **等今天这几场开赛后，真 CLV 的"收盘版"第一次会真实存在**
  （以前是 0/82 可算；今天起分母不再恒为 0）。
## 15. ★★ 09:13–09:30 北京：国内机**当场把清单写坏**，同时**盘口全历史到货**（一好一坏，同一批回传）
**坏的那件（每 10 分钟在扩大）**：`01-13-20Z` 起，`.done` 第一列**丢了 `topic/` 前缀**——
```
01-03-20Z.done : "jczq_offer/2026-09-16T01-03Z__001.jsonl\t40\t<hash>"   ← 正常（619B→703B 之差就在这 12 个字符/行）
01-13-20Z.done : "2026-09-16T01-13Z__001.jsonl\t40\t<hash>"              ← 无目录前缀
01-23-18Z / 01-30-20Z：同样坏（截至 09:30 已 3 批；且节奏从 :33 漂到 :30 ⇒ 他们大概在手动改东西）
```
· 真文件是好的（`jczq_offer/2026-09-16T01-13Z__001.jsonl` 100,533 B / 40 行）⇒ **纯粹是清单写错**；
· 我方 2o 的装载器**按契约拒收**（无法归属的文件不入库）⇒ **北京 09:13 / 09:23 / 09:30 三批的 40 盘口 + 9 赛果 + 23 篮彩 + 120 大乐透全没进**，
  `fact.jc_offer.max(snap_ts)` 停在 `01:03:02Z`，假 gap 8→15；
· **两条处置**：① 已回给国内机（inbox「紧急」：一行改回 `<topic>/<文件名>`；顺带 `.done` 是 CRLF、hash 列挂着 `\r` ⇒ 建议统一 LF）；
  ② 我方不依赖他们改：2q 加"**无前缀 ⇒ 按 topic 目录消歧（存在 + 声明行数吻合才算）+ 打违约日志**"，消歧失败仍 `rejected`（宁可不插）⇒ **T8 要求把 01:13 真装回来**（`ups` 从 0 变 49）。
**好的那件（超出我的预期）**：09:12 他们把**盘口全历史样本**直接放进落港目录 `odds_history_sample/`（+`接口说明.md`）——
 正是我 08:17 那份需求单要的三样：**端点全名 + 参数/头要求 + 2 场真实响应（1 完场 1 在售，9 键外壳、payload 未裁）**。
· 我方 09:20 逐字段实测 ⇒ **冻结契约 v1.4**（`docs/project/契约v1.4-盘口全历史.md`）：五个数组 `hadList/hhadList/crsList/ttgList/**hafuList→haf**`、
  两份样本 **34 个价格版**（7/9/6/6/6）、**严格新→旧**、**0 组时间戳撞键**、`src_hash` 复算**两行全一致**、时间是**北京墙钟**、`*f` 箭头 `1/0/-1`；
· 唯一要他们改的一点：样本里 `topic` 写成了 `jczq_offer` ⇒ **v1.4 要求改 `jc_odds_history`**（否则撞名会被当普通盘口解析）；
· DB 侧我已经做完（不等人）：**新表 `fact.jc_odds_history` 已执行**（`docs/db/infra_p0_11_odds_history.sql`，14 列 / PK / 3 CHECK / 2 索引 / 三角色授权），
  **故意不建 FK**（样本两场 `2041482/2041495` 不在 `fact.jc_match`，且封闭清单不含这些联赛 ⇒ §9-50 先测覆盖率再说）；`jc_offer.odds_history` 那列**标废弃、永久 NULL**；
· 工单拆开：**3b** = 端点全历史 → 新表（真 CLV 正路，oracle 全取实测数）；**3a** = offer 包里内嵌的那段 `oddsHistory` → `jc_offer.odds_history`（零依赖、现在就能做）；**两者数据源不同不可混用**。
· ⚠️ 范围纪律（用户 06:35 的封闭清单）：样本两场是**卡塔尔亚/韩国亚/柏太阳神**（亚冠/日职）⇒ **只用于证明形状与跑测试**，照入库可以（盘口不是预测），
  但**永不进模型与预测**；解析器**禁止写 `ref.team`**（队名对照是 D3 人工审的活，`seed_jc_team_map.sql` 还没过）。
**验收侧的两条教训（已进 §9）**：**62** 逐批期望值必须逐批现算（我 E3 写"各 49"被真数据撞破，那几批 `jczq_result` 是 `.empty`）；
 **63** 工人跑到一半的代码写进 `ops.*` 的脏行不会被正确的重跑撤销 ⇒ 伪 gap 只能超级用户删（`119/14 → DELETE 6 → 113/8`，剩 8 条逐条点名为老三批真缺）。
## 16. ★★ 09:30–10:46 北京：把"对方写坏清单"从**停数据**降级成**只脏账本**，并挖出派工链路的真故障
### ① 数据侧：清单消歧上线，实时水重新流动
· 契约 v1.4 + 新表 + 两张新工单（**3a/3b**）落好后，我用 `~/bin/jc-recon-batches.py`（新工具：按"该 topic 目录下真有此文件且行数吻合"消歧，
  在 `/tmp/recon` 里**重建带前缀清单 + 符号链接**，再用**现有装载器**跑）应急装了 `01-13/01-23` 两批 ⇒ `ups=49`×2、`gaps` 不增；
  **落库的 `src_file` 与代码消歧后的结果完全同构**（`topic/文件名`），所以**不产生第二套口径**——这是敢在修好之前先救数据的理由。
· 然后 **2q1b 验收通过**（工人 15 分钟没交报告，我按亲验收）：`jc_read.py` 83→99 行；
  实时目录 7 个丢前缀批**全部 `ups=49`、`errors=[]`**；`fact.jc_offer` **690→970**、`distinct snap_ts` **15→22**、
  `max(snap_ts)` 从停在 `01:03:02Z` 到 **`02:03:01Z`**（= 北京 10:03，一个 10 分钟档的正常滞后）；`pytest` **428 passed**。
· **E6 是我输的**（队长错账 #9）：工单只写了"消歧未命中不记 missing"，**没写"命中要撤销先前那次预记"** ⇒
  `_from_manifest` 先把七个 topic 全判成缺档、再把消歧结果 append ⇒ **数据进库了、假 gap 照记**，`ops.file_arrival` 假 gap **8→57**。
  修法已定死在 **2q2 C2**（"先消歧完、再一次判 missing"，结构唯一）；57→8 我用 `~/bin/jc-false-gaps.py --sql --delete` 清了
  （判据不靠猜：0 字节 marker 的 gap 一律视为真；新格式清单列过该 topic ⇒ 判伪；**首版我拿"清单首段是不是 topic"判，正好被丢前缀坑成 28 条全判错**）。
### ② 派工链路：连续三单"零写入"的真因**不是**模型偷懒，是 **ACP 会话静默死亡**
· 证据：三次尝试（12 / 14 / 2 分钟）日志尾巴**全都有 `[ACP 无返回]`**、`.attempts` 记 `rc=0`、`max_tries=1` ⇒ 直接 `EXHAUSTED`；
  死亡时 `usage_update: used=29443 / size=524288` ⇒ **不是上下文撑爆** ⇒ "把工单写得更短"根本救不了它（我白拆了两遍工单，还在报告里错怪了模型）。
· 修了三条（§9-64）：**诊断顺序固定**（`.attempts` → 日志尾 → 才怪工单）；**`max_tries≥3` 且只认 `.done` 哨兵**（`rc=0` 不算成功）；
  派工器加 **`LEAGUE_WATCH_PATHS` 脏检查**——并且**"脏"不等于危险**：10:43 那次 2q2 断流时它已经做完了搬家（`jc_read.py` 99→36 + 新模块 68 行），
  只是断在改文件头 ⇒ 留了个**字符串没闭合的半个文件**，守卫正确地 `DIRTY-ABORT`；我把半成品存档到 `/tmp/2q2-*`、还原 HEAD，
  守卫升级成"**脏的 .py 全部可编译才准续跑，不可编译才 abort**" ⇒ 10:45 重派（3 次 × 20 分钟）。
### ③ 顺手补的两个洞（都不是工单里的）
· **`fact.jbq_*` 三张表在库里活了两天，但它的 DDL 文件从未进过 git**（我当年在 `/tmp` 执行完就直接开工单）⇒
  从运行库 `pg_dump` 反向重建 `docs/db/infra_p0_10_jbq_tables.sql`，并在**临时 schema + `begin…rollback`** 里重放验证
  （**表 3 / 约束 19 / 列 61**，与运行库逐项相等，rollback 后零残留）；新守卫 **`~/bin/jc-ddl-audit.py`**（在库每张基表都要能在 `docs/db` 找到 `create table`，
  缺一张 exit=1；当前 **38/38**；**反证做过**：把补回的文件挪走 ⇒ 准确点出那三张）⇒ 规矩进 §9-65"**DDL 与执行同一提交**"。
· **真 CLV 的前置卡点其实不是代码，是球队对照**：`jc_write._team()` 只认 `ref.team.aliases->>'sporttery'`，
  实测该键 **0/122** ⇒ `fact.jc_match` 的 **17 场 `home/away_team_id` 全 NULL** ⇒ 预测/回测/CLV 全部开不了工。
  我把 34 个队位逐位判读写成 **`docs/project/待审-竞彩球队对照.md`**（✔唯一 8 / ◻疑似 4 / ✗无候选 22；**封闭清单内只有 14 支**，
  其中 3 支"对不上"其实是**我方 `ref.team` 池子不全**，西甲就缺），并给用户 A/B/C 三个方案（默认 A=按清单扩池，等人工审）。
## 17. ★ 10:52–11:07 北京：cron 预演通过 + **第一个盘口走势实测结果**（顺手，但影响 CLV1 的设计）
### ① 装载定时入口在"cron 那种环境"下预演通过（真跑，非纸上判断）
· `env -i HOME=/home/ubuntu PATH=/usr/bin:/bin bash ~/bin/jc-ingest-run.sh` ⇒ **rc=0**：venv 激活 ✔、`flock` 单实例 ✔、
  **`sudo -n -u postgres psql` 在无 TTY 下也能跑**（本来担心 `requiretty` 卡住自检）、24 批一次追平：`fact.jc_offer` **970 → 1130**、`distinct snap_ts` 26、
  最新水 `02:48Z`（北京 10:48，一个档的正常滞后）；盘/库余量：库 134 MB、落港目录 13 MB、`/` 剩 182 G（6%）⇒ **容量完全不是问题**。
· **但 cron 仍然不装**，理由变了而且很具体：C2 那个 bug 让每个丢前缀批**多写 7 条假 gap** ⇒ 现在 `ops.file_arrival` 假 gap 已到 **85**（真 8）。
  装上 cron = 每天 144 批 ⇒ **一天灌 ~1000 条垃圾进缺档账**，看板健康条（DASH2）会天天假报警。⇒ **2q2/2q3 修完假 gap 再装**。
### ② 第一个"盘口走势"实测（17 场 × 5 玩法 × 26 个自有快照）
```
play_type | 线数 | 平均版数 | 最多版 | 有变动 | 最大振幅
hhad      |  17  |  13.3   |  26   |   3    |  0.20
had       |  14  |  14.0   |  26   |   3    |  0.11
有 ≥5 个快照的场次 = 8
```
· 只统计了带 `h` 键的 `had/hhad`（`crs/ttg/haf` 的键是 `sXX…`，**不是没动**，是这条查询按 `'h'` 过滤天然排除 —— 别把它读成"那三个玩法零变动"）；
· **结论（影响 CLV1 设计）**：**开售前官方价基本不动** —— 9 小时 26 个快照里只有 **6/31 条水位线**动过，振幅 ≤ 0.20；
  ⇒ **拿"我方 10 分钟快照序列"当收盘价来源是薄弱的**（大多数场次从头到尾只有一个价），
  真正的走势与封盘价必须来自**官方 `getOddsHistoryV1`（契约 v1.4：一场 5–8 版，含封盘那一版）**——这正是我今晚把 3b 排到队首的原因；
· ⚠️ 另一个前提仍未满足：**球队对照 0/122**（详见 §16③ 与 `docs/project/待审-竞彩球队对照.md`），
  在它点头之前 `fact.jc_match.home/away_team_id` 全 NULL ⇒ **预测/回测/CLV 都出不了数**，只能先把数据攒对。
## 18. ★★ 11:13–11:41 北京：**假 gap 根因修掉 ⇒ 装载 cron 正式装上**（数据从此自动跟 10 分钟档）
### ① 大单作废、拆微单：`2q2`（三步）两轮零写入 ⇒ `2q3`（一个函数）25 分钟落地
· `2q2` 第二轮到 11:06 还是 **`rc=124`（超时被杀，会话活着但一个字没写）** ⇒ 与 §9-64 的"断流"是**两种死法**（一种单太大、一种链路抖），
  我 11:12 kill 掉它，改成**只改一个函数**的 `2q3`（把 `_from_manifest` 从"先判 missing 后消歧"倒过来）。
· `2q3` 时间线（工人视角，`.attempts` + trace）：11:13 派 ⇒ 11:19 **落笔** ⇒ 11:21 跑 `jc-ingest-run.sh` ⇒ 11:23–11:28 跑夹具与 pytest
  ⇒ 11:28 超时（**没交报告**，跟 `2q1b` 一样）。我 11:30 亲验收全绿 ⇒ **`9642995` ACCEPT**；`jc_read.py` **99 → 97 行**（**反而更短**）。
· **派工器这一轮第一次真"续跑"**：`.attempts` 出现 **`RESUME-DIRTY try=2 脏文件均可编译 ⇒ 允许续跑（1 个）`**
  ⇒ §9-64 的"半个文件才拦、自洽就放行"这条改法**在实战里跑通了**（10:43 它拦下的是字符串没闭合的半个文件，判得对）。
### ② 验收（我逐字复跑，不采信工人报告）
```
静态      97 行（≤100）· omp-ast-check 0 违规 · py_compile · import ingest.jc_read      ✔
T1 实时   bash ~/bin/jc-ingest-run.sh → rc=0 · jc_offer=1440 无重复增长 · gaps 跑前 91 / 跑后 91（零新增假 gap）✔
真缺档没被一起关掉：03-15-24Z 残清单批的 6 条 gap 仍在（`jc-false-gaps.py` 逐条判"盘上也没有该文件 = 真缺档"保留）  ✔
T2/T3     --dir /tmp/incoming → ups=15 · --dir /tmp/incoming2 → ups=40 · errors=[]                        ✔
T5        jc-fk-audit.py fact → exit=0 · pytest tests -q → **428 passed / 19.8 s**                        ✔
账本清理  jc-false-gaps.py --sql --delete ⇒ **91 → 14**（历史 77 条假 gap 清零；14 条真缺档保留）           ✔
```
· ⚠️ 我自己的手滑记一条：`--delete` 单独用**不生效**（工具写死"须与 `--sql` 同用"），我第一次只给 `--delete` ⇒ 它**只打印审计、一行没删**（默认安全，判它设计对）。
### ③ **cron 正式装上并且真跑通了**（目标 ③ 达成）
· `*/10 * * * * bash /home/ubuntu/bin/jc-ingest-run.sh >> /home/ubuntu/logs/ingest-cron.log 2>&1`（装前 `env -i` 预演过，见 §17①）；
· **第一次自动运行 `03:40:04Z`（北京 11:40）**：`rc=0`、自检行 `jc_match=36 jc_offer=1440/29 jc_result=24 gaps=14` ⇒ **gaps 没长**（= 修复生效）；
· 之前手动追平的量：`fact.jc_offer` **690 → 1440**、`distinct snap_ts` **15 → 29**、最新水 **`03:15Z`（北京 11:15）** ⇒ 看板的水现在是**自动**跟的；
· **为什么之前一直不装**：C2 那个 bug 每批多写 7 条假 gap ⇒ 144 批/天 = **每天约 1000 条垃圾缺档**，看板健康条会天天假报警（修完当天就装，没多等）。
### ④ 采集机又新增一种违约 + 我方"无主文件"比原来报的多
· **残清单**：`2026-09-16T03-15-24Z.done` 只有 **99 字节 / 1 行**（只列了 `jczq_offer/…135 行`）⇒ 其余 6 个 topic 我方**只能按真缺档记账**
  （契约 v1.2 起就写死 `.done` 是最后一步）⇒ 已进 inbox（要求"传完再写、一次写完；没采到也要写 `<topic>/none.empty	0`"）。
· **开售包变大**：11:03 批起 `jczq_offer` 单批从 **40 行涨到 135 行**（336 KB）⇒ 我方照常入库，**预测范围仍按封闭清单**。
· **无主文件我重测了一次**：粗算（文件名前 16 字 vs marker 前 16 字）有 **35 个**"无主"，但这个口径**不权威**
  （早期批次 `.done` 的上传时刻 ≠ 文件的采集分钟，窗口分支会认领一部分）⇒ 权威口径要等 `2q3b` 的 orphan 日志 `orphan 装载 n=?`。
· ⇒ **`2q2` 的 D 步（orphan 兜底）从"优化"升级成"必须有"**，已单独成单 **`P0-COLLECT2q3b`**（11:40 派，只改 `jc_load.py`，净增 ≤12 行），
  判据里最关键的一条是 **T3 幂等：连跑两次第二次必须 `n=0`**（否则就是"文件永远不会有 marker"的契约问题，那要停下找我，不许自己加去重表）。
## 19. ★★★ 11:40–12:07 北京：**orphan 兜底上线**（今天第一张"报告 + 哨兵双全"的工单）· cron 两Tick 正常
### ① 装载 cron 真跑通两个 Tick（目标 ③ 完成）
| 时刻(北京) | `fact.jc_offer` | distinct 快照 | gaps | 说明 |
|---|---|---|---|---|
| 11:40:04 | 1440 | 29 | 14 | 装上后第一次自动运行，`rc=0` |
| 12:00:04 | **1575** | 30 | 14 | 第二次自动运行 ⇒ **新批次(+135 行开售包)自动进库、假 gap 零增长** |
⇒ **看板的水从现在起自动跟 10 分钟档**，不再需要我手动跑装载器（今天 09:30 之后一直是我在手动追，追平了 45 分钟滞后）。
### ② `P0-COLLECT2q3b`（orphan 兜底）落地并且**今天第一次由工人自己交齐报告 + `.done` 哨兵**
· **权威"无主数"是 14 不是 35**：我 11:49 用真代码量（159 个 `jsonl`、清单/窗口认领 145）⇒ 无主 14，
  全部集中在 `09-15 22:48 / 22:49 / 22-51 / 22:54` 四个采集分钟——**这四分钟对方根本没写 `.done`**（第三种违约形态）；
  其中包含 **3 个 120 行的 `lottery_draw`** 与 **3 个 40 行的 `jczq_offer`** ⇒ **这些是从未入库的真数据，本单把它们捞回来了**。
· 实现按我的 B1–B4 一字不差（`_ops` 拆 `done` 参数、`load_topic` 加 `"orphan"` 三态、`main()` 末尾 `seen` 集合、日志只一条）⇒ `jc_load.py` **87→99 行**（≤100 ✔）。
· 工人 12:06 交 **115 行报告**，并且在我那条写错的判据（"第二次 `n` 必须 0"）上**没有为了让测试变绿而自己加去重表**——
  它按工单【边界】的要求**把两次的 `n` 和计数原文贴回来等我定**（这是今天最好的一次处置，值得记进手册）⇒ 我 11:51 已把判据更正为
  "**幂等 = 不新增行**"（§9-71 队长错账 #11：孤儿本来每次都会重新捞，走 upsert 无害）。
· 我亲验：`orphan 装载 n=14` ✔、`done_marker=false 且非 gap` 的 arrival 行 = 14(+1 测试残留，已 `DELETE 1` 清干净) ✔、
  夹具目录 `n=0 文件=-` ✔、`gaps` 跑前跑后相等 ✔、ast 0 违规 ✔、`jc-fk-audit` exit=0 ✔、`pytest` **428 passed** ✔ ⇒ **`17dd0af` ACCEPT + push**。
### ③ 今天累计的账（截至 12:07）
· **已验收并 push**：`P0-COLLECT2q1b`（清单消歧）· `P0-COLLECT2q3`（假 gap 根因，99→97 行）· `P0-COLLECT2q3b`（orphan 兜底，87→99 行）；
  外加队长侧：`fact.jc_odds_history` 建表（`infra_p0_11`）· 契约 v1.4 · `infra_p0_10`（jbq DDL 补档）· 4 个新工具 · 12 条错账/教训（§9-60~71）。
· **采集机三种违约同时在场**：① 清单**丢 `topic/` 前缀**（12:00 批仍在丢）；② **`.done` 半传半写**（`03-15` 只剩 1 行 ⇒ 6 个 topic 真缺档）；
  ③ **整批没写 `.done`**（09-15 那 4 个分钟 ⇒ 14 个无主文件）。三道都由我方"消歧 + 窗口 + orphan 兜底"接住，**但根治必须对方改**（已进 inbox 两条）。
· **接下来（不打扰用户，按队列走）**：`P0-COLLECT3b1`（盘口全历史**纯解析器**，12:07 已派，8 组硬断言）⇒ `3b2`（写手，测试全程 rollback）
  ⇒ `P0-JCSPLIT`（`jc_load.py` 纯搬家，为接线腾行数）⇒ `3b3`（接线）⇒ `2q3c`（清单残行防崩）⇒ `2e`（传统足彩五表）⇒ `STORE2`（篮彩）。
· **仍然等用户的一件事**：**球队对照 / 扩池 A·B·C**（`docs/project/待审-竞彩球队对照.md` + `docs/db/待审-seed_jc_team_alias_sporttery.sql`，
  草案 8 条已备好、**一行都没执行**）⇒ 在它点头之前，**预测/回测/真 CLV 出不了数**（看板 `backtest` 那行 `clv_filled=0` 就是这个卡点的直接体现）。
## 20. ★★ 12:07–12:40 北京：盘口全历史**解析器**落地（435 passed）· 我今天连写错三条判据
### ① 交付
· **`P0-COLLECT2q3b`（orphan 兜底）ACCEPT** → `17dd0af`（见 §19）。
· **`P0-COLLECT3b1`（v1.4 盘口全历史纯解析器）ACCEPT** → `3e34d84` + 更正 `967f30c`：
  `scripts/store/parse_odds_hist.py` **74 行** + `tests/test_parse_odds_hist.py`（7 个用例）⇒ 全量 `pytest tests -q` = **435 passed**（428 + 7）。
  关键点：**`hafuList → 'haf'`** 的名字映射、`seq_no = len(arr)-1-i`（**0 = 最早 = 开盘**，因数组实测"最新在前"）、
  `odds` 剔除三个元数据键、`is_close` **恒 False**（§9-70）、`leagueId` 是**字符串 `'83'`** 才转 int、**不 import 任何库相关东西**（T4 用 `grep import` 自证）。
  ⇒ 而且它**先按北京解释墙上时钟、再 `astimezone(utc)` 归一**，比我的规格更干净（这也是下面 #14 那条错账的由来）。
· 队长侧：`P0-COLLECT3a` **作废**（同一事实两个家；`jc_offer.odds_history` 已注释为弃用，将来要审计"当时看到的历史尾部"就加 `seen_at` 列）→ `334072a`。
· **cron 健康**：12:00 Tick `jc_offer 1440→1575`、`gaps` 不涨；12:38 我再跑 `jc-false-gaps.py` ⇒ **判伪 0 条 / 保留 20 条**（20 条全是真缺档，含两次"残清单"批各 6 条）。
### ② 我今天**连写错三条自己的判据**（都已在手册里立规矩，不重复犯）
| # | 我写错的判据 | 真相 | 立下的规矩 |
|---|---|---|---|
| §9-71 | "幂等 ⇒ 第二趟 `orphan 装载 n` 必须 0" | 孤儿**定义**就是"永远没 marker 的文件"，每趟都会重新捞；走 upsert ⇒ **不新增行**才是幂等 | 判据要写在**HEAD 代码先跑一遍**之后，不写"我以为的行为" |
| §9-72 | 看 liveness 用 `…_try1_*.log` 的 mtime | 模式里写死 `try1` ⇒ **永远拿第一轮旧日志**，我把**活着的 try 2 当死的杀了**（它已流到 1.05 MB） | 新工具 **`~/bin/omp-live.sh <TAG>`**：`ls -t <TAG>_try*_*.log \| head -1` + 静默秒数；>180 s 才算停 |
| §9-73 / #14 | "`tests/**` 也卡 100 行" / "`utcoffset()` 必须是 0" | 仓库既有测试 175/135/128/124 行（**早验收过**）；aware `datetime` 的 `==` 比**时刻**、`utcoffset` 只是偏移标签 | `omp-ast-check.py` 改成 **tests 免文件上限**（函数/列宽照查，**双向验证过**：106 行测试 0 违规、101 行生产码仍报违规）；跨时区断言**只断 UTC 瞬间** |
· 另外记一笔**流程事实**：`3b1` 这张单**断流两次**（`try 1` 只活 **67 秒**、`try 2` 被我误杀），**`try 3` 在超时前 30 秒交齐报告 + `.done`**
  ⇒ 今天**第二张"双全"工单**；`max_tries≥3` 这条从"理论保险"变成了**实际救回两张单**（另一张是 `2q3b` 靠 `RESUME-DIRTY` 续跑完成）。
### ③ 现在的状态一句话
**数据链路已经自己会走路了**（cron 每 10 分钟 + 三层容错：消歧 / 窗口 / orphan；账本 20 条 gap 全真）；
**盘口全历史只差"写手 + 接线"**（`3b2` 12:38 已派，测试全程 `begin…rollback` 保证 `fact.jc_odds_history` 最终仍 0 行）；
**仍然只有球队对照（A/B/C）在你手里**——`backtest` 那行 `clv_filled=0` 就是它的直接体现（`docs/project/待审-竞彩球队对照.md`）。
## 21. ★★ 12:40–13:25 北京：搬家完成（装载器腾出 37 行）· 我今天**又**写错两条判据（#15/#16）· `3b2` 卡在 psycopg3 的 savepoint 上
### ① 交付与验收
· **`P0-JCSPLIT2`（`jc_load.py` 纯搬家）ACCEPT** → `9213c76` + 更正 `fc75d21`：
  `_ops`/`load_topic` → 新文件 `jc_topic.py`(44)，`jc_load.py` **99 → 62 行**（净腾 **37 行**，正好给 3b3 接线用）；
  纯度我用 `jc-move-check.py` 自证（`MATCHED … --- OK: 纯机械搬移`），行为我也自证（`rc=0`、`orphan n=14` 不变、**gaps 跑前跑后都是 20**、`fk-audit` 0、既有 **435 passed**）。
· **`P0-COLLECT3b1`（解析器）ACCEPT**（见 §20）→ `3e34d84`；`P0-COLLECT3a` 作废 → `334072a`；`P0-JCGATE1`（封闭清单闸门）新写并已 13:23 派工。
· **今天第三、四张靠 `RESUME-DIRTY` 续跑救回的工单**：`3b2`（断流 2 次：12:59 与 13:21）与 `JCSPLIT2`（12:59 断流）都是从"已有半成品"原地续跑，**没有从零重来**。
### ② 我今天写错的第 15、16 条判据（都改了工单，不重复犯）
| # | 我错在哪 | 现场 | 立的新规矩 |
|---|---|---|---|
| §9-75 | 工单里**写死数据库计数**（`jc_offer=1440 / gaps=14 / 428 passed / orphan n=0`） | cron 每 10 分钟装载 ⇒ 到 12:46 全部过期；`orphan n=0` 还跟我 12:04 自己立的 §9-71 正面矛盾 ⇒ 派出去 40 秒我自查出来、**立刻收回重派** | 会动的数一律 **E0 自量基线 + 只比不写死**；只有**离线夹具**可写死 |
| §9-78 | 我写"逐行 `try/except` 就不炸批" | **同一 PG 事务里一报错就 aborted**，后面全报 `InFailedSqlTransaction` ⇒ 工人照我的错规格写，必然红 | 容错写手必须写死**错误恢复原语**：逐行 **裸 SQL savepoint**（`savepoint` / `release` / `rollback to`） |
| §9-79 | 写手工单只写了一句 `with closing(pg.connect("ing"))` | 测试同时红在 `the connection is closed` 与 `assert 0 == 34` | **连接生命周期**必须写死：一个 conn、断言全在 close 前、rollback 前后查数不混用；**先证明"关掉容错能插 1 行"** |
· **`3b2` 的真因是我 13:22 亲自抓出来的**（不是工人写错）：`pytest -k t2 --log-cli-level=WARNING` 抓到被容错吞掉的原文
  ⇒ `Connection.transaction() got an unexpected keyword argument 'savepoint'` —— **psycopg3 根本没有这个关键字**（§9-80）。
  已写进工单 + 手册；工人现在被要求改裸 SQL savepoint。
### ③ 一条你会关心的新事实（解释看板为什么一直没有真预测）
· 12:53 逐 `league_id` 量了今天在售的 36 场竞彩足球：**封闭清单内只有"西甲 9 场"**（`league_id=62`），
  其余 27 场是 欧罗巴 12 / 解放者杯 4 / 英联赛杯 3 / 亚冠精英 2 / 亚运 4 / 荷甲 1 / 巴甲 1 / 英冠 1 ⇒ **`/api/jc/issues` 一直 0 行不是链路坏，是"没得预测"**（红线：清单外只当走势资料）。
· 顺带纠正我自己一次误判（§9-76）：我一度以为对方联赛命名"一名两写"，其实是我把 `league_cn`（全称）与 `league_abbr`（缩写）**混在一个 union 里**看错了 ⇒ **闸门今后认 `league_id`/`league_abbr`，中文名只做展示**。
### ④ 仍在进行的
`P0-COLLECT3b2`（写手，try 3/3，13:22 起）· `P0-JCGATE1`（闸门，try 1/3，13:23 起）· 之后依次：`3b3` 接线 → `2q3c` 清单防崩 → `2e`（传统足彩，已标注"必须先搬家"的前置）→ `STORE2`（篮彩）→ `DASH2`（健康条 + 日期下拉 + `day=garbage` 必须 400）。
## 22. ★★★ 13:25–14:30 北京：写手落地（**全量 441 passed，今天第一次整套全绿**）· 我自己的工具**跨工单误杀**被抓现行并修好
### ① 交付（今天第 8、9、10 张验收单）
· **`P0-JCGATE1`（封闭清单闸门）ACCEPT** → `2443851`：`scripts/core/jc_gate.py` **29 行**；我把 10 组真实 `(league_id, league_abbr)` 自己喂了一遍 ⇒
  命中集合**恰好 `{(62,'西甲')}`**、`NBA/CBA → True`、**`'西甲联赛' → False`**（模糊匹配堵死）、`ALLOW_IDS` 只有 `{62}`（工人没自己乱填 id）。
· **`P0-COLLECT2q3c`（毒清单防崩）ACCEPT** → `86c0662`：`jc_read.py` 97→99 行；我拿四行毒输入（只一列 / 空列 / `xx` / 重复正常行）直接喂 `_manifest()`
  ⇒ **不抛**、毒行得 `decl=-1`（"声明不可知"）走 `manifest-mismatch`，**零库内残留、gaps 前后都是 20**。
· **`P0-COLLECT3b2c`（盘口写手）ACCEPT** → `a17f798`：`jc_odds_write.py` 51 行 + 测试 85 行；
  **`pytest tests -q` = 441 passed / 0 failed**（= 439 + 2，**这是今天下午第一次整套全绿**），跑完 `fact.jc_odds_history` 仍 **0 行** ⇒ "**测试全程单事务、最终不留行**"自证成立。
  这张单前后花了 **4 轮会话**（`3b2`×3 + `3b2b`×1 被我主动终止 + `3b2c` 一轮过）——**三轮都卡在我自己写错的规格上**（§9-78 savepoint / §9-79 连接生命周期 / §9-83 连接所有权），
  最后一轮我把修法写到"照抄级"（`cur` 不是 `conn`、保存点本轮先建后用），新会话 **2 分钟就绿** ⇒ **结论：工单写到位，这个模型就够快**。
### ② 🔴 我今天最贵的一次自我发现：**我的派工器会跨工单杀会话**
· 现场：13:43:34 `P0-COLLECT3b2` 自己超时的**同一秒**，`P0-JCGATE1` 与 `P0-COLLECT2q3c` 双双 `rc=15`；13:52:03 又反过来（gate 结束、2q3c 被杀）；
· 根因（`omp-resilient3.sh:34/38`）：清理"孤儿"用的是**本轮启动时的全局 pid 快照** ⇒ **任何比本轮晚启动的 omp 进程都被当孤儿杀掉**，其中就包括**别的工单**；
· 修法（已写 **`~/omp-resilient4.sh`**，今天 14:00 起所有新派工都走它）：`setsid timeout … & TP=$!` ⇒ 收尾只 `kill -TERM -$TP`（**timeout 自己就是组长**，组里恰好只有本轮 omp-call/acp/worker）
  ⇒ 我拿 `setsid bash -c 'sleep 30 & sleep 30'` 单独验证过组语义（`kill -TERM -$TP` 后残留 0）；
· 顺带定**并行纪律：同时最多 2 张单**（13:2x–13:4x 三单并行那 10 分钟里断了 3 次会话）。
### ③ 一条"差点误判成回归"的记录（§9-82）
我工单里的夹具判据只写了目录没写批次：`--dir /tmp/incoming` 里躺着 **3 个 marker**，`15-16-32Z → ups=15`、`15-03-45Z/14-40-02Z → ups=85`
⇒ 我 13:55 拿 `14-40-02Z` 跑出 85、对不上记忆里的 15，**差点把 2q3c 判成回归**；把三个批次各跑一遍才确认**装载器行为完全正确**、错在我那条**不完整**的判据
⇒ 四张在用工作单已当场补成 `(目录, --batch)` 二元组（`/tmp/incoming2` 的正确配对其实是 `--batch 2026-09-15T23-01-30Z → ups=40`）。
### ④ 球队对照：按最新在售数据**重量了一遍**（这直接改变你 A/B/C 的性价比）
· 14:05 实测：现在 36 场里**封闭清单内只有西甲 9 场**（`league_id=62`）⇒ 涉及 **18 支官方队**，对我方 122 支池子逐字唯一化：
  **唯一命中 11 支 · 译名可合 1 支（678 巴伦西亚 = 2347 瓦伦西亚）· 池子里根本没有 6 支（阿拉维斯 / 马德里竞技 / 莱万特 / 拉科鲁尼亚 / 马拉加 / 桑坦德竞技）**
  ⇒ **"连马竞都不在池子里"**：说明池子对今天的竞彩盘子是真不够，不是个别漏队；
· 于是三个选项在**今天这份数据**上的实际产出算成了表（写进 `docs/project/待审-竞彩球队对照.md` 末段，inbox 也已换成新数）：
  **C（只入 11 条唯一命中）⇒ 立刻可预测 3/9 场**（今天就能看到真预测与真 CLV）· **A（补齐池子，默认）⇒ 审完 9/9** · **B（只补这 6 支）≈ 8–9/9 但换个联赛又缺**。
### ⑤ 现在
`P0-COLLECT3b3`（盘口全历史**接线**，净增 ≤14 行，走 `OPTIONAL` 语义防 143 条假缺档）14:30 已派，走 **V4** 派工器；库里 `jc_offer=2385 / 36 快照`、`gaps=20`（判伪 0）、`jc_odds_history=0`（等接线才有第一行真数据）。
## 23. ★★★ 国内采集机回话的四件事，我**逐条在真盘上验**（14:4x–15:0x）——三条成立、一条没修好
| 他们说的 | 我的独立验证 | 结论 |
|---|---|---|
| ① `getOddsHistoryV1.qry`（`channel=c&matchId=`，UA `Mozilla/5.0`，不需 Referer/Cookie）已探明，2 场真样按 9 键外壳**零裁剪**推到 `odds_history_sample/`，未接定时 | 目录真在（`接口说明.md` + 两份 `.jsonl`），外壳 9 键齐、五个数组齐；喂我已验收的解析器 ⇒ **34 行 = had7/hhad9/crs6/ttg6/haf6、`is_first` 10、`goal_line` 非空 9** ✔ 与我的 oracle 一字不差 | ✅ **但 `topic` 仍写 `jczq_offer`** ⇒ 接定时任务时目录与 topic 都要改成 `jc_odds_history`，否则我会拿普通盘口解析器解全历史包 |
| ② 无主文件是 23:01 前 v1.1 旧全量遗留，10 分钟档首跑不覆盖该窗口 ⇒ 以后不再出现 | 我库里 `done_marker=false` 是 **14 个**（不是 3 个），来自 09-15 **22:48/49/51/54 四个整批没 `.done`** 的分钟（含 3 个 120 行 `lottery_draw`）；已被 orphan 兜底全部捞进库 | ⚠️ 数量对不上，但"不再出现"我接受——**由第④条的哈希校验来证明** |
| ③ `push_batch` 已改 **paths 驱动**（tar 与清单一一对应、不多不少、无 double-list） | 最新三批（`03-55` / `04-43` / `06-43`）**每批正好 7 行、7 个 topic 都有文件** ⇒ "一一对应"为真 ✔；**但第一列仍然全部丢 `topic/` 前缀**，且七行**字面完全相同** | ❌ **前缀没修好**（最重要的未决项）：今天全靠"按目录消歧 + 行数对账"接住（2385 行 / 37 快照零丢失），可**两个 topic 行数一旦相同就是猜** ⇒ 踩红线 |
| ④ 级联哈希 = `sha256(逐行 utf-8 字节按序拼接)`；`.empty` 第三列留空 | 我按这个定义重算：**每批第 1 行精确命中**；后 6 行"不命中"**是我自己探测错了**（丢前缀 ⇒ 七行同名，我拿同一个 offer 文件比了七次）；改成按 7 个 topic 目录分别算再反查 ⇒ **7/7 全命中**，并量出他们的**固定行序** `1 jczq_offer · 2 jclq_offer · 3 jczq_result · 4 jclq_result · 5 jc_issue · 6 jc_issue_result · 7 lottery_draw` | ✅ **定义完全正确**（§9-85）⇒ 第三列第一次变成可用：**(a) 哈希精确消歧**（替掉"猜行数"）、**(b) 半传检测**（`03-15-24Z` 那种 99 字节事故可自动发现） |
**新发生并已恢复的一件事**：他们 **12:43–14:43（北京）整条链路停了 1 小时 53 分**（我方 cron 三趟全 `rc=0`、orphan 数一条没涨 ⇒ 确认上游停摆，不是我没读）；
**14:43 恢复、14:50 那趟已载入**（快照 36 → **37**）。停摆期间我方**不记缺档**（"没清单=没清单"的保守口径，§9-67）⇒ `gaps` 仍是 **20**、判伪 0 条。
**由这条回复直接排出的两张单**（已写好、按序执行）：`P0-JCSPLIT3`（把清单四件套从 99 行的 `jc_read.py` 纯搬家到 `jc_manifest.py`，34 行区间我按 AST 量好了）→ `P0-COLLECT2q4`（**哈希精确归属 + 半传告警**：①带前缀照旧 ②丢前缀且有哈希⇒命中才插、0 个或 ≥2 个都**不猜**并进 `errors` ③老包无哈希⇒退回按行数零回归；**明令禁止依赖那个行序**，他们随时会改）。
## 24. ★★★ 16:08–16:36：今天下午最值钱的一刀——**哈希精确归属断根"假缺档每趟虚涨"**
`P0-COLLECT2q4c` 落地并验收（`e325060`）。病根链条：采集机清单丢了 `topic/` 前缀 + **多个 topic 声明同一行数**（15:03 起篮彩 25 撞足球 25）
 ⇒ 老"按行数消歧"命中 ≥2 ⇒ 按红线**不猜** ⇒ 每批 2~4 条被记成假缺档 ⇒ **`gaps` 一上午从 20 虚涨到 38、`orphan 未落 marker` 从 14 虚涨到 32**。
**修复 = 用采集机 `.done` 第三列的级联哈希**（我今天 14:52 独立复算 7/7 命中，§9-85）：`jc_manifest` 的 `_resolve` **先按哈希精确归属**（命中唯一才返回，0 或 ≥2 都不猜、只打告警关键字），
 老包没有哈希（`.empty`/`undefined`）**原样退回按行数档 ⇒ 零回归**；带前缀的正常路径**刻意不吃哈希**（不动已验证语义）。
**真包规模的决定性证据**（我亲手跑，非工人报告）：一趟装载 **`哈希消歧=440 / 无法消歧=0 / hash-mismatch=0 / hash-ambiguous=0`**；
 假孤儿 `orphan 未落 marker` **32 → 14 自愈**；判伪删掉坏窗口的 18 条假缺档后 **gaps 回到真值 20，且从此不再虚涨**（根修已进主干 + cron 在跑）。
装载报警脚本 `~/bin/jc-ingest-run.sh` 现每趟打印这四计数，`mismatch/ambiguous>0` 直接指名"这批别自动装载"（半传/篡改实时哨兵）。
**过程账**：`P0-COLLECT2q4` 我第一版写成了"**做不到的要求**"（§9-94：`_manifest` 把哈希列丢了，调用方又在禁改的 `jc_read.py`），
 停轮、自纠、把 **P1~P5 逐字写死**重派 `2q4c` ⇒ 工人一字不差、try 1 就全绿 —— 又一次印证 §9-84：**照抄级小单 1 轮过，巨型/含错单 3~4 轮零产出**。

## 25. 18:00–18:35：盘口历史已经在入库（我先前以为还没通）+ 球队对照 A 方案草案验过

### 盘口历史：`fact.jc_odds_history` = 331 行 / 27 场 / 5 玩法齐全
- 采集机从 `2026-09-16T09:53Z` 起就在推 `jc_odds_history/`（5 个批次），**目录名与 topic 名都已是 `jc_odds_history`**
  （我早先担心它还叫 `jczq_offer`，实测已改好，这条不用麻烦采集机了）；
- `ops.file_arrival` 里 5 个批次全在、`done_marker=true`（09-53/09-58/10-03/10-13/10-23Z，行数 25~27）
  ⇒ **3b3b 验收的写手 + `jc_topic` 的 `jc_odds_history` 分支已经在生产路径工作**，只是走的是"无主文件"通道；
- 玩法分布 `had=79 ttg=70 crs=63 haf=61 hhad=58`（27 场 × 平均 12 条赔率变动）；
- **只读演练**（`/tmp/oh_rehearse.py`，ing 角色 + 收尾 rollback）：真包 25 行 env → 写手返回 `ups=270`，
  证明"marker 加一行就能落库"这条路无需任何代码改动。
- **要给采集机的一条**：把这 5 行加进每批 `.done` marker（`jc_odds_history/<批次>.jsonl<TAB>行数<TAB>哈希`），
  它就能走正常批次归属 + 哈希校验，不再算无主文件。

### 采集机已兑现的两条（不用重复交代）
最新 marker `2026-09-16T10-23-53Z.done` 里 7 行**都已带 `topic/` 前缀**、**第三列都是哈希**，
与 §9-95 的"哈希消歧=538 / 无法消歧=0 / mismatch=0"互相印证。

### 球队对照 A 方案（用户批复「池子开始可以大，后面可以收缩」）
草案 `docs/db/待审-seed_jc_team_pool_v2.sql`（**未执行**，D3）：西甲 12 条 alias + 英超 3 条 + 6 条 INSERT + 回填；
名字全部取自本库 `fact.jc_match` 官方中文名（零外部依赖、零猜测）；
**事务内验过再回滚**：`sporttery 键=21 / 新增队=6 / 西甲对齐 9/9 / 清单内未对齐 0`（现状 0/9）。
三个缺口已写在文件里：6 支新队 `af_id` 为 NULL 无历史特征；其余五联赛+CBA/NBA 本版不做（今天清单内 0 场）；清单外 27 场故意不建。
过程踩坑：`ref.team.team_id` 是 identity GENERATED ALWAYS（`ref.team_team_id_seq`，last_value=120311），手填值会报错 ⇒ 改为交给 identity 自增。

## 26. 19:20–20:30：球队对照 A 已执行 + 三个写指令 topic 真入库 + 修 7 个被真数据打破的测试

### ① 球队对照 A 已执行（用户批复原话「执行」）
`docs/db/待审-seed_jc_team_pool_v2.sql` → 改名 `seed_jc_team_pool_v2.sql`（去掉待审前缀），
单事务 BEGIN…COMMIT 原子落地：15 UPDATE（西甲 12 alias + 英超 3）+ 6 INSERT 缺池新队 + 回填 UPDATE。
回执：`ref.team 122→128`、`sporttery 键=21`、`jc_id 非空=21`、序列 `120505→120511`（恰好 +6）、
**西甲 0/9 → 9/9 对齐**、清单内联赛 9/9、全库 10/36。
新队 `team_id 120506~120511`（阿拉维斯 171 / 马德里竞技 172 / 莱万特 33 / 拉科鲁尼亚 509 / 马拉加 512 / 桑坦德竞技 676），
`af_id` 全 NULL（缺口①，没有 AF 历史特征，不能当「已可预测」）。
**顺带发现**：清单外多对齐 1 场 = 联赛杯 利物浦 vs 热刺——因为英超 3 条 alias 落下去后球队身份自动解析，
联赛本身不在封闭清单所以仍不会被预测。这正是「池子先大」的预期效果。
**补幂等**：6 条 INSERT 原先无守卫（重跑会被 `partial unique (sport,jc_id)` 打断，我上一轮误说「可反复执行」是错的），
已改 `not exists`；全量重跑验证 `15×UPDATE 0 / 6×INSERT 0 0 / 回填 UPDATE 0` 全 no-op。
过程中一个坑：`'{\"sporttery\":\"171\"}'` 在 `standard_conforming_strings=on` 下不是合法 JSON（反斜杠被当字面量），
必须写 `json.dumps` 出来的纯引号形式。

### ② 三个写指令 topic 真入库了（P0-COLLECT2eB 接线 ACCEPT）
`jc_topic.py` 48→63 行：新增 `ISSUE=("jc_issue","jc_issue_result","lottery_draw")`，
沿用 `jc_write` 的 `parse_line→upsert` 姿势，每行 `savepoint iw` 隔离 + 只 catch `ValueError`，
`parse_line` 返回 `None` 跳过。生产已生效：**`fact.jc_issue=8` / `fact.lottery_draw=120`**。
顺带纠正我两个猜错的名字：`fact.jc_issue_result` 不存在（真名 `fact.jc_issue_draw`）；
`fact.jc_odds_history` 的更新时间列不叫 `updated_at`。

### ③ 修 7 个被真数据打破的测试断言（我修的，fixture 属队长职责）
套件从 `450 passed + 7 failed` 回到 **457 passed / 0 failed**。7 个失败全是一类：断言绝对行数。
cron 今早起真往这三表装数据 ⇒ 测试的隔离假设不成立了：
- `test_jc_issue_write`：合成 `issue_no 26127/26128/26001` 与真实期号撞车（`count` 从 1 变 2）
  ⇒ 加 `RUN=uuid` 唯一前缀；两处整表 `count(*)==1` 改按 `issue_no` 限定；
  finale 从「三表必须 0 行」改成「本模块写的三个键不残留」（语义不变，仍验回滚是否生效）。
- `test_jc_odds_write`：夹具 `match_id 2041482/2041495` **就是真实期号**（cron 也用这两个 id，
  `match_id in (...)` 命中 61 行）⇒ 每次运行生成唯一 match_id 基址并原地重写 `payload.matchId`，
  全部计数按这组 id 限定；两处 ro 检查同样改按键查残留。
- `test_jc_topic_issue`：finale 从「三表 0 行」改成「开测前快照 vs 收尾快照逐表相等」
  （该用例自开自收连接，不走 `conn` fixture，才能拿到自己的回滚边界）。
**教训**：测试夹具的键必须每次运行唯一，不能写死成看起来像真实值的常量——
一旦生产表开始装真数据，「表必须为空」这类断言必然假失败。

### ④ 两张单的工人**同一秒**断流（基础设施问题，不是工人各自失败）
`P0-COLLECT2eB`（第 3 轮）与 `P0-COLLECT2lqP`（第 1 轮）的日志**都停在 11:20:30 的半个 token**，
`omp-live` 显示 0 进程、静默 >180s。⇒ 派新单没用，先等基础设施恢复。
`2lqP`（篮彩赛果解析，单已定稿 `tickets/P0-COLLECT2lqP.txt`，102 行照抄级）尚未开工，待重新派发。

## 27. 13:20–13:40：采集机两条答复已验真盘（推翻我此前两处判断）+ 端到端入库验证 PASS

### ① marker 不用补 —— 我此前的判断是错的
我原先说「marker 里没有 jc_odds_history 那行 ⇒ 只能走无主通道」。实测推翻了这句话：

| 批次类型 | .done 份数 | 含 jc_odds_history |
|---|---|---|
| 8-topic（含 history） | **12** | ✔ 第 8 行 |
| 7-topic（不含 history） | **63** | ✘ |
| 空 .done（09-15 凌晨） | 3 | — |
| 单行无前缀（前缀 bug 活证据） | 1 | — |

节奏切换点**精确落在 11:04→11:22**：最后一份 8-topic = `11-04-57Z`，第一份 7-topic = `11-22-46Z`，
与采集机说的一致。所以 11:22 之后 ~2 小时没有新盘口历史进来是**设计如此**（10m 档 7 topic 防堆积死机），
不是 marker 漏写、也不是我们入库坏了。

**端到端验证（刚做）**：13:30 一份 8-topic 批次刚到，我手动跑 `jc-ingest-run.sh`：
- `fact.jc_odds_history 348 → 396`
- `ops.file_arrival` 里 13-30 批次 **8 个 topic 全部入库**（含 jc_odds_history）
- `jc_offer 8130/81 → 8490/84`
- 哈希校验一致：marker 里 `2e38f2324aabb10b…` = `sha256(逐行去换行后拼接)` 复算结果 ✔
  （我第一次核对用了 `sha256sum` 对带换行的原始字节，算出 `89001104…` 对不上，**是我最错**，
  口径见 `scripts/ingest/jc_manifest.py:13`；文件 mtime 13:30:54 早于 marker 13:31:08，未被重写）

**顺带定位**：`jc_odds_history` 不在 `jc_load.py:11` 的 `TOPICS` 元组（7 topic）里，
而是在 `scripts/ingest/jc_read.py:8` 的 `OPTIONAL = ("jc_odds_history",)` 走可选通道 —— 走通了，
但只看 TOPICS 会漏掉它。

### ② 312 个无主文件 —— 根因用硬数字钉死，且我们这边**无需任何动作**
采集机说 312 个文件不在任何 .done 内（根因 = 09:58 前批次清单路径无 topic/ 前缀，修复 commit `d3c65f4`）。
我按自己的口径重数：

- marker 内唯一路径 = **312**（与采集机口径一致）
- topic 目录 jsonl 总数 = **535**，不在任何 .done 内 = **292**
- **marker 里 281 行的首列不含 `/`**（缺 topic/ 前缀）← 根因的活数字

分布：jczq_offer 46 / lottery_draw 45 / jc_issue_result 45 / jc_issue 44 / jczq_result 42 /
jclq_result 42 / jclq_offer 28；jc_odds_history **0**（它的 12 份全部正常批次归属）。

**关键**：我们的 loader **本来每次 run 就全量重扫**，不用等「下轮」——
`scripts/ingest/jc_load.py:42-44` 无条件算出「所有 topic 目录 jsonl − 已在 marker 内的 = 无主」，
`:47` 逐个走 `load_topic(..., "orphan")` 通道（`done_marker=false` 标记来源）。
上一轮日志已见 `orphan 装载 n=14`，`ops.file_arrival` 里已有 **34 行 done_marker=false**
（jczq_offer 4 / jczq_result 3 / jc_issue 7 / jc_issue_result 6 / lottery_draw 6 / jclq_offer 3 / jclq_result 5）。
哈希消歧也全绿：`哈希消歧=538 无法消歧=0 hash-mismatch=0 hash-ambiguous=0`。

⇒ **结论：采集机修复前缀后我们不用补历史 .done、也不用专门重扫，cron 每 10 分钟自动收。**
唯一设计外目录 `odds_history_sample/`（2 个 jsonl + 采样包）不属批次产物，忽略即可。

## 28. 19:40–20:30：AF 开了但白开（免费档只到 2024）+ 揪出球队对照回填被 cron 每 10 分钟冲掉 + 看板补「入库与对照」四段

### ① 用户说「要开」AF——开了，但白开
按 D9 开关路径实测：`run_backfill.py --plan --seasons 2026 --source af` 计划 5 条
（epl/laliga/bundesliga/seriea/ligue1），配额 100/100 未动；`--execute` 五条全 HTTP 200 落块。
但每块 body 都是：
```
{"get": "fixtures", "errors": {"plan": "Free plans do not have access to this season, try from 2022 to 2024."}}
```
**AF 免费档硬限 2022–2024 赛季**，当前赛季（2026）要付费档。5 次请求各耗 1 配额（已用 5/100，余 95）。
⇒ 「开 AF 拉西甲当前赛季」这条路不通，需要用户决定：买付费档 / 拉西甲次级联赛 2022-2024
（能补上 4 支降级队的历史）/ 接受这 4 支只有竞彩侧身份。

顺带纠正一件我自己记错的事：`API_FOOTBALL_KEY` 不在环境里——env 里实际叫 `API_FOOTBALL_API_KEY`，
而 `scripts/config.py:41` 的 `key_env` 写的是 `API_FOOTBALL_KEY` ⇒ **密钥名不匹配，AF 一直取不到密钥**。
AF 标为 `paid=False`，所以不需要 `--allow-paid`。跑通是靠运行时注入 `API_FOOTBALL_KEY=$(grep …)`。

### ② ★更大的事：球队对照回填被 cron 每 10 分钟冲掉
§26 我回填过 `fact.jc_match.home/away_team_id`（西甲 9/9），但真盘实测现在**三个 business_date 全是 0 对齐**
（2026-09-15: 9 场 0 对齐 / 09-16: 17/0 / 09-17: 10/0）。原因两颗雷：

- **根因一** `scripts/ingest/jc_topic.py:37` 硬编码 `upsert_jc_match(cur, p["match"], snap, src, None, None)`
  ⇒ `ON CONFLICT DO UPDATE` 每轮把 `home/away_team_id` 覆盖回 NULL。回填是治标，入库才是根。
- **根因二** `jc_write._team()` 早已写好「sporttery id → `ref.team(team_id)`」解析，
  但**零调用方**（死代码），而且参数写错：
  `cur.execute(sql, [str(i) for i in ids])` 把列表拆成 N 个标量参数，SQL 只有 1 个 `ANY(%s)`
  占位符 ⇒ 任何双边有 id 的比赛都会 `ProgrammingError`。
  **我接上它之前先直测了解析器，抓到这颗雷**——不测，cron 会整条炸。

修复（热修，coder 基础设施 11:20:30 断流 + 数据每 10 分钟在被销毁，不能等）：
`_team` 参数补外层括号 + `jc_topic.py` 调用 `_team`。
验收：第一次入库 **0/36 → 10/36**（西甲 9/9 全对齐、英联杯 1/3）；第二次入库仍 10/36（幂等）。
36 场里只有已映射的 21 支能解析，正确行为。已写工单 `tickets/P0-JCTEAM2.txt` 收尾
（`_team` 改公开 API、删 `upsert_jc_result` 的死参数 `home/away`、补 team_id 填充与 cron 冲刷回归测试）。

### ③ 看板补「入库与对照」四段（P0-DASH2c）
新增 `scripts/store/jc_ops_view.py`（93 行）+ `web/routers/jc_ops.py`（46 行），
`GET /api/jc/ops` 一次取四段，每段独立降级（store 异常该键 `ok=false`，不 5xx）：

| 段 | 口径 | 实测 |
|---|---|---|
| teams | 按联赛聚合**全部**待售比赛日 | 西甲 9 场/3 天/9 对齐/3 可预测 |
| topics | file_arrival 按话题 + 距今分钟 | 8 topic，jc_odds_history 44 分钟（daily，预期） |
| quota | 当日各源余量 | api_football 5/100，余 95 |
| rejects | 近 24h，只认 `rejected` jsonb 非空与 `ok=false` | 空（全绿） |

⚠️ **rejects 口径踩坑**：不能用 `rows_in - rows_ups` 判拒行——重放未变更、按设计跳过
（jclq 2e 范围）都会让它变正。实测把 `lottery_draw` 算成 **369360** 行「被拒」；
真盘核验全库 43283 行 `ingest_log` 的 `rejected` 全为 NULL、`ok` 全为 true ⇒ **真拒行 0**。
空表 = 全绿，不是降级。

⚠️ **teams 口径踩坑**：不能只看 `max(business_date)`——清单跨 3 天，
只看最新一天（2026-09-17 只有 10 场）会漏掉 26 场。

前端 `static/jc.html` 加 4 段（球队对照/入库健康/数据源配额/近 24h 拒行），
时间戳统一转北京时间显示；`static/index.html` 头部加「竞彩看板 →」入口。

### ④ 顺带修 DASH2-D：`fixtures?day=garbage` 200 → 400
原先非法日期静默降级成空表（200）。同时发现**验收脚本本身测错了端点**：
`~/bin/dash-verify.sh` 把 `day=garbage` 发给 `/api/jc/issues`（该端点无 `day` 参数，
FastAPI 忽略未知参数 ⇒ 恒 200），**掩盖了 fixtures 端点根本没做校验这个事实**。
已改测 fixtures 并标注 issues 端点行为；另修脚本在非交互 shell 下 venv 激活失效
（`python: command not found` ⇒ 登录 401 全线假失败）⇒ 改显式 `$PY` 路径。

### 验收
- `PYTHONPATH=scripts pytest tests -q` → **457 passed**（无回归）
- `dash-verify.sh` 全绿：四端点未登录全 401；登录 200；ops `degraded=False` 四键全 ok；
  `fixtures?day=garbage / 2026-13-40 / 2026/09/16` 全 400，合法日期 200；残留 uvicorn = 0
- 提交 `c448ad5`，已推 `origin/v2`

## 29. 20:30–20:50：用户批复 C（接受 AF 缺口）+ 顺手查出一个更要紧的库内事实

### ① C 决策已落盘
`tickets/P0-TEAMMIX1.txt` 范围收窄：删除原「方案 1 合并 SQL」项——AF 拉不到当前赛季 ⇒ 合并永远 no-op，
写它没有意义。本单改只读核查 + 留档。看板 `static/jc.html` 的 teams 段 note 也标注了该决策，
让缺口在看板上自明（差值 = 已对齐 − 可预测，就是这批）。

### ② ★查出一个更要紧的事实：竞彩侧同一家俱乐部有两个 sporttery id
准备做那 2 支「免费补 af_id」时先查了数据，结果发现这根本不是一个能合并的问题：

```
皇家马德里  id=2350   af=541  jc=664  aliases={"sporttery":"664","api_football":541,"football_data":86}
马德里竞技  id=120507 af=NULL jc=172  aliases={"sporttery":"172"}
```

`fact.jc_match` 实测：`home_sporttery_id=172` 时队名「马德里竞技」；
`away_sporttery_id=664` 时队名「皇家马德里」——**采集机自己用两个名字指同一家俱乐部**。

⇒ 不是能靠合并解决的：`aliases->>'sporttery'` 是**单值**，一行只能持一个 id；
`jc_id` 生成列也只取一个值。合并等于丢掉另一个 id 对应的那部分比赛——**有损**。
真正修法是 schema 支持「一队多 jc id」（DDL，另开工单），或采集机统一队名口径（采集机侧）。

顺带确认库内**无法**模糊检测同类情况：`ref.team` 没有 AF 原名列（只有 `name_cn`+`aliases`），
按 `name_cn` 分组查不到（两个名字字符串本身不同）。检测需要队名归一化，
属 `store/team_identity.py` 的 `resolve_team` 范畴，不在 TEAMMIX1。

**我没有自作主张合并**——D3 球队映射需要人工批复，而且这个合并有损。
已把这个事实写进工单顶部（决策更新段），并转给采集机确认队名口径。

### 验收
- `node` 校验 `static/jc.html` 内联 JS 语法 OK；`dash-verify.sh` 全绿
  （ops `degraded=False` 四键全 ok，残留 uvicorn = 0）
- 提交并推 `origin/v2`

## 30. 22:30–23:10：采集端回复推翻我的判断（我错了）+ 真正的重复对只有 2 对 + 踩了一个查约束的坑

### ① 我错了：172 和 664 是两家不同俱乐部
采集端回复：「沿用 sporttery 官方字段即已统一——`homeTeamAllName`/`awayTeamAllName`（全名）
进队表、`*AbbName`（简称）做展示名，两俱乐部各一行，各挂各的 id。」

真盘复验：
```
Atletico Madrid   id=2354  af=530 fd=78 jc=-      被 fact.fixture 引用 114 场
马德里竞技         id=120507 af=-   fd=-   jc=172
皇家马德里         id=2350  af=541 fd=86  jc=664
```
场次：09-15 埃尔切 vs **皇家马德里**(664)、09-16 **马德里竞技**(172) vs 奥萨苏纳(534)
——两场不同比赛。**皇家马德里 = Real Madrid，马德里竞技 = Atlético Madrid，同城对手。**
我把两者搞混了，前一条报告里的「竞彩侧同俱乐部双 id」结论作废，采集机无需改口径。

### ② 真正的重复对只有 2 对（西甲 9 场全景）
| 场次 | 状态 |
|---|---|
| 皇家贝蒂斯(543) vs 赫塔费(546) | ✔ 两边都有历史 |
| 埃尔切(797) vs 皇家马德里(541) | ✔ |
| 巴列卡诺(728) vs 西班牙人(540) | ✔ |
| **马德里竞技**(jc=172) vs 奥萨苏纳(727) | 缺左 → **AF 行在库：Atletico Madrid af=530，114 场** |
| **阿拉维斯**(jc=171) vs 巴伦西亚(532) | 缺左 → **AF 行在库：Alaves af=542** |
| 马拉加(512) vs 比利亚雷亚尔(533) | 真降级，无历史 |
| 巴塞罗那(529) vs 桑坦德竞技(676) | 真降级，无历史 |
| 拉科鲁尼亚(509) vs 塞维利亚(536) | 真降级，无历史 |
| 莱万特(33) vs 毕尔巴鄂竞技(531) | 真降级，无历史 |

全库 substring 扫查，无误报外无同类重复（`威尼斯`/`尼斯`、`拉斯帕尔马斯`/`帕尔马` 是含字误报）。
⇒ 与 C 决策一致：**4 支确实无历史，只有 2 支能免费合并。**

### ③ 合并方向必须反过来（TEAMMIX1 原方案 1 写反了）
预测通过 `fact.fixture` 连历史，历史挂在 **AF 行**（Atletico Madrid 114 次、马德里竞技 0 次）。
所以 winner 必须是持 af_id 的 AF 行、loser 是我插的 sporttery 行——与原方案「winner = 持
sporttery 键那一行」**相反**。照原方案合并，join 还是打不到历史。

### ④ 踩坑：查约束的 SQL 报错，我误读成「零约束」
`pg_constraint where conrelid='ref.team'` 的 `text || char` 报错返回空，我据此断言「零约束」。
干跑立刻被打回：`duplicate key value violates unique constraint "team_jc_key"`。
实际 `ref.team` 有 4 个**部分唯一索引**——它们不是 pg_constraint 项，`pg_constraint` 查询**看不到**：
```
team_jc_key            UNIQUE (sport, jc_id)  WHERE jc_id IS NOT NULL
team_af_key            UNIQUE (sport, af_id)  WHERE af_id IS NOT NULL
team_fd_key            UNIQUE (sport, fd_id)  WHERE fd_id IS NOT NULL
team_sport_name_cn_key UNIQUE (sport, name_cn)
```
⇒ 合并顺序必须**先摘 loser 的 sporttery 键、再加到 winner**，反了立刻撞 team_jc_key。
另：`team_merge_map` 在 `scripts/` `web/` `tests/` **零引用**（4 行先例全是死数据），
写进去只为审计，不影响 join。

### ⑤ 待审 SQL 已干跑验证
`docs/db/待审-西甲两对合并.sql`：前置断言 + 先摘后加 + merge_map 留痕 + 三条验收查询。
干跑（`sed 's/^commit;$/rollback;/'`）输出：
```
A) 2354  Atletico Madrid | af=530 fd=78 jc=172  三键齐
   3114  Alaves          | af=542 fd=263 jc=171 三键齐
   120506 阿拉维斯       | {} 空壳孤儿
   120507 马德里竞技     | {} 空壳孤儿
B) 重复 jc_id：0 行
```
回滚后库里未变（4 行原状、merge_map 仍 4 行）。**未落库，等人工点头。**
执行后需重跑 `bash ~/bin/jc-ingest-run.sh`（`fact.jc_match` 的 team_id 由我的热修自动重解析）。
