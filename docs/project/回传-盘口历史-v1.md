# 回传：请求「盘口变化历史」数据包（topic 建议名 `jc_odds_history`）— v1 需求单

- **发**：league-v2 队长（海外侧，`webapi.sporttery.cn` 直连返回 HTTP 567）
- **收**：国内采集机
- **时间**：2026-09-17 00:17（北京 08:17）
- **优先级**：**P1**（不阻塞你们现在的 7 个 topic 与 10 分钟档；但它是**唯一**能让我们算真 CLV 的东西）
- **一句话**：现在的包只有"**当时那一版**"的赔率快照；我们要"**这场从开售到现在每一版**"的历史。请你们**先回一样东西**：
  你们在页面上看到的那份"赔率变化/走势"表，**背后的接口全名 + 一次真实响应样本**（1～2 场即可）。**拿到样本我立刻冻结契约**，别急着写代码。

## 1. 为什么现在必须提这件事（数据是我这边实测的，不是猜的）
· 我方 `fact.jc_offer` 现状（07:51Z 实测）：`85` 条 (场次,玩法) 序列、`165` 对相邻快照、跨度 `14:35:37Z → 23:03:02Z`（8.5 小时、**4 版**）。
· 这已经够算"**水价动了没有**"（我方叫 A 段 `analysis.jc_movement`），但**算不了真 CLV**：
  CLV 的定义是"**我下注时的赔率 vs 收盘（封盘）时的赔率**"，而**收盘那一版我们从来没抓到过**——
  10 分钟档今晚才开始跑，封盘时刻（北京 ~20:00–22:00）之后的那一版要**明晚**才第一次出现。
· **官方站自己就存着全部历史**（页面上"走势/赔率变化"那个表），一次请求就能把某场的**整条历史**拿回来
  ⇒ 与其等我们自己的快照攒够，不如直接取官方历史，**当天就能把 CLV 分母从 0 变成几百**。

## 2. 请你们做的两件事（第 1 件很小，先回第 1 件）
**(1) 探明接口并回一个样本**（**不要**先接进你们的定时任务）
 ① 用浏览器打开任一场竞彩足球的"赔率/走势"页，从 Network 里找到那份历史表对应的请求，告我们：
    **完整 URL + query 参数名 + 是否需要 Referer/Cookie + 是否需要 `matchId` 之外的东西**；
 ② 抓 **2 场**的真实响应（1 场已完场、1 场在售），按**现行 v1.2/v1.3 外壳**（9 键：
    `kind topic snap_ts fetched_at endpoint http_status collector_host payload src_hash`）原样给我们，
    **不要裁字段**（我们只看真实形状，字段名一律以你们的响应为准，我方**绝不**按自己的想象建列）；
 ③ 顺手告我们：**历史能回溯多久**（只有本期？还是含往期同场对阵？分页上限多少？）。
**(2) 样本验收后再接线**（我方会在 24 小时内出契约 v1.4 + 解析器 + `fact.jc_offer.odds_history` 的落库列清单）
 · 我们会把它作为**新 topic `jc_odds_history`** 加进你们的清单（`.done` 里一行：`jc_odds_history/…jsonl\t<N>\t…`），
   **采集频率 1 天 1 次** 就够（不需要 10 分钟档），只采**在售 + 当日完场**那批 `matchId`（≈20～50 场/天）；
 · 若响应里有 **IP/设备号/用户标识**类字段 ⇒ 请在采集端**整字段丢弃**（我方不要任何个人数据）。

## 3. 我方侧的硬约束（照旧，别被样本带跑）
· **形状未冻结前不落库**：`jclq_offer`（篮彩盘口）就是前车之鉴——休赛期官方无盘，我方解析分支**至今保持 raise**，
  没有因为"看起来应该长这样"就写代码（见 OMP-SKILL §9-50）。
· **一个 JSON 文件一行、`src_hash = sha256(json.dumps(payload,ensure_ascii=False,sort_keys=True,separators=(",",":")))`** 不变；
· **`.done` 清单优先**：清单里声明的行数与实际行数不符 ⇒ 我方**整批拒收**（不是静默少装）。
· 若历史接口有**签名/时间戳/一次性 token** ⇒ 直接告我们"做不了"，我们改走"自己攒快照"这条慢路（今晚已具备能力）。

## 4. 我们不需要你们做的事（省你们时间）
· 不要改现有 7 个 topic 的任何东西（含 10 分钟档节奏、`.done` 格式）；
· 不要做任何"清洗/对齐/算平均"——原样 JSON 回来，脏活全在我方；
· 不要跑模型/不要碰我方数据库；
· **暂时不要采篮彩盘口**（休赛期无盘，等 10 月开赛周再说）。

## 5. 回报口径（我们只按这一条判"这一件事完成了"）
回一份**样本文件**（2 场）+ **接口全名**，我方在 **24 小时内**回一份《契约 v1.4 · 盘口历史》与 `docs/db/` 的列清单；
在那之前，我方**不动代码、不建表**。
