# M0 报告 — 引擎数据契约勘察（第一批 M0a）

## 完成清单

| 节 | 状态 | 备注 |
|---|---|---|
| §1 CLI 全景 | ✅ 完成 | 参数表、副作用流、stdout/stderr 约定均有代码证据 |
| §2 预测 JSON schema | ✅ 完成 | 顶层 keys、predictions[] 字段、past_matches[] 字段、calibration 字段均覆盖 |
| §3 文件命名与时区 | ✅ 完成 | BJT 时间戳、文件名不含 league、同小时覆盖、选取规则伪代码 |
| §4 赛果与回填 | ✅ 完成 | results schema、关联键三条路径、各模块分工 |
| §5–§8 | ⏳ 待补 | 留待 M0b（AI 富化链路/数据源配额/联赛代码/i18n） |

本轮实际交付：
- `docs/web/DATA_CONTRACT.md` 第 1–4 节（无残留「待补」字样）
- 本轮对先前会话遗留内容进行了核验与事实校正

## 证据密度自评

| 结论 | 密度 | 说明 |
|---|---|---|
| CLI 参数与副作用 | 高 | predict.py:51-77, 82-84, 169-181, 233-387, 390-447 多处直接行号引用 |
| 预测 JSON schema | 高 | 样本文件 + predictor.py:326-365 + parse.py:327-361 交叉验证 |
| 文件命名与时区 | 高 | predict.py:171-172, 280/329, 422, 438/446 精确到行号；样本 generated_at 对比佐证 |
| 赛果回填关联键 | 高 | backtest.py:17-99, output.py:31-74, calibration.py 多处行号 |
| `to_cn` 回退英文队名 | 高 | i18n.py:103-110 + parse.py:258/280-281 + 样本实证三证合一 |
| `--no-fetch` 语义误导 | 高 | predict.py:82-84 + help text |
| `--all` 同名覆盖规则 | 高 | predict.py:171-172, 431-444 |

整体：1–4 节结论证据密度高，均为直接代码行号或 JSON 样本片段支撑。

## 本轮修正（核验发现的事实错误）

1. **`match`/`home`/`away`/`name` 为英文队名串**（非中文）—— `to_cn` LLM 不可用时回退原英文名。
   - 修正 §2.2、§2.3、§4.2、§4.3 相关断言
   - 证据：parse.py:258/280-281, i18n.py:103-110, 样本 `prediction_2026-07-21_10.json:22/27-28`

2. **`poisson_top3` 概率和 < 1**（仅 Top3 子集）—— 原描述「概率和为 1」错误
   - 修正 §2.2
   - 证据：predictor.py:268/282, 样本中 Top3 概率和 = 0.416

## 未尽事项

- §5–§8 全部内容留待 M0b 阶段（AI 富化链路、数据源与配额、联赛代码表、i18n）
- §9 疑点清单已有 9 条，但部分条目（LLM 消耗源、`--no-fetch` 缓存依赖）需 M0b 细读 fetch.py/cache.py 后补充定量估算

## 疑点清单（同步进 DATA_CONTRACT §9）

见 `docs/web/DATA_CONTRACT.md` §9（9 条），本轮未新增，仅修正其中关于「中文队名」的错误断言。

## 完成时间

- M0a 主体工作由先前会话完成
- 本轮（2026-09-09 13:30 UTC）完成核验与事实校正，落盘 M0_report.md 与哨兵
