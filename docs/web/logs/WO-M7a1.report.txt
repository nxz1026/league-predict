WO-M7a1 报告
===========
工单：删除两个零引用死模块

零引用验证：
- grep -rn "ai_enrich_gha" ai scripts web tests --include='*.py'：仅命中 scripts/ai_enrich_gha.py 自身、ai/feedback_loop.py 的文档字符串（流程描述注释）、web/services/jobs.py 与 web/routers/jobs.py 的 docstring 注释、tests/web/test_m6_ai_enrich.py 的断言文本（断言要求脚本名不含 ai_enrich_gha.py，实际调用 web.enrich）。无真实 import（grep import 零命中）→ 满足删除条件。
- grep -rn "feedback_memory" ai scripts web tests --include='*.py'：零命中 → 满足删除条件。

删除：
- rm scripts/ai_enrich_gha.py
- rm ai/feedback_memory.py

测试：
- 命令：PYTHONPATH=scripts:. /root/venvs/web/bin/python -m pytest tests -q
- 删除后：192 passed, 2 warnings in 14.98s
- 删除前基线：上一轮全量 192 passed（本工单未在删除前单独跑，删除前计数以仓库既有基线 192 为准；删除零引用模块后计数不变，仍 192 passed）

测试副作用（非本工单改动，未触碰）：
- git status 另见 docs/web/logs/STATUS.md（外部心跳日志进程写入）与 predictions/ai_scores.json（既有进程写入），均与本次删除无关。
